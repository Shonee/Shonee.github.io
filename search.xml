<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Daily Hard (1)</title>
    <url>/daily-hard/</url>
    <content><![CDATA[<p> <a href="https://leetcode-cn.com/problems/insert-interval/">leetcode 57.插入区间</a> </p>
<p><strong>题目描述：</strong></p>
<blockquote>
<p>给出一个无重叠的，按照区间起始端点排序的区间列表。</p>
<p>在列表中插入一个新的区间，你需要确保列表中的区间仍然有序且不重叠（如果有必要，可以合并区间）。</p>
</blockquote>
<p>**示例1： ** </p>
<blockquote>
<p><strong>输入：</strong> <code>intervals = [[1,3],[6,9]], newInterval = [2,5]</code> </p>
<p><strong>输出：</strong> <code>[[1,5],[6,9]]</code> </p>
</blockquote>
<p><strong>实例2：</strong> </p>
<blockquote>
<p><strong>输入：</strong> <code>intervals = [[1,2],[3,5],[6,7],[8,10],[12,16]], newInterval = [4,8]</code><br><strong>输出：</strong> <code>[[1,2],[3,10],[12,16]]</code><br>**解释： ** 新的区间<code>[4,8]</code> 与给出的区间集合中的区间有重合部分，取所有重合部分的并集合，得到最终无重合有序区间集合。</p>
</blockquote>
<p><strong>解题思路：</strong></p>
<p>插入一个新的区间，需要考虑的是新的区间是否和现有的区间有重叠的部分，已知现有的区间集合中所有区间无交叉且有序，则</p>
<blockquote>
<p>从头遍历区间数组，比较新区间的右端点与所有区间的左端点，来定位新区间的右边界；比较新区间的左端点与所有区间的右端点，定位新区间的左边界；</p>
<p>遍历时有如下判断：</p>
</blockquote>
<ul>
<li>创建一个存放区间的集合，进行区间的添加</li>
<li>如果新区间的左端点 &gt; 当前遍历区间的右端点：<ul>
<li>加入当前区间，迭代下一个区间进行判断</li>
</ul>
</li>
<li>如果新区间的右端点 &lt; 当前遍历区间的左端点<ul>
<li>则要加入新区间，设定完成标识，迭代加入余下区间</li>
</ul>
</li>
<li>都不满足时，新区间和现有区间有重叠，合并重叠的区间<ul>
<li>合并区间时左端点选择两个左端点中较小的一个</li>
<li>合并区间时右端点选择两个右端点中较大的一个</li>
<li>合并成一个区间后作为新区间去迭代完成遍历</li>
</ul>
</li>
<li>如果遍历完成后，新区间仍然没有完成添加，则最终加入新区间</li>
</ul>
<p>算法代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//执行用时：1 ms, 在所有 Java 提交中击败了99.48%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：40.6 MB, 在所有 Java 提交中击败了92.52%的用户</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[][] insert1(<span class="keyword">int</span>[][] intervals, <span class="keyword">int</span>[] newInterval) &#123;</span><br><span class="line">    <span class="keyword">if</span>(newInterval.length == <span class="number">0</span>) <span class="keyword">return</span> intervals;</span><br><span class="line">    List&lt;<span class="keyword">int</span>[]&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">    <span class="keyword">int</span> l = newInterval[<span class="number">0</span>], r = newInterval[<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">boolean</span> flag = <span class="keyword">false</span>;   <span class="comment">//记录新集合是否已经添加进去，true的话后续不用比较</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span>[] interval : intervals)&#123;</span><br><span class="line">        <span class="keyword">if</span>(interval[<span class="number">0</span>] &gt; r)&#123;</span><br><span class="line">            <span class="keyword">if</span>(!flag)&#123;</span><br><span class="line">                list.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;l,r&#125;);</span><br><span class="line">                flag = <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            list.add(interval);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(interval[<span class="number">1</span>] &lt; l)&#123;</span><br><span class="line">            list.add(interval);</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">//此时，当前集合与新集合一定有交集的，合并称为一个集合作为新集合</span></span><br><span class="line">            l = Math.min(l,interval[<span class="number">0</span>]);</span><br><span class="line">            r = Math.max(r,interval[<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//如果遍历结束后新集合仍然没有加入，则最后加入</span></span><br><span class="line">    <span class="keyword">if</span>(!flag) list.add(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;l,r&#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将list中的集合转到二维数组进行结果返回</span></span><br><span class="line">    <span class="keyword">int</span>[][] res = <span class="keyword">new</span> <span class="keyword">int</span>[list.size()][<span class="number">2</span>];</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; res.length; i++)&#123;</span><br><span class="line">        res[i] = list.get(i);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;        </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>数据结构与算法</category>
      </categories>
      <tags>
        <tag>Hard</tag>
        <tag>Algorithm</tag>
        <tag>Daily</tag>
        <tag>数组</tag>
        <tag>排序</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo+Typora写文章时图片显示问题</title>
    <url>/testimage/</url>
    <content><![CDATA[<p>Hexo</p>
<a id="more"></a>

<h2 id="Typora"><a href="#Typora" class="headerlink" title="Typora"></a>Typora</h2><blockquote>
<ul>
<li>Typora是一款支持实时预览的MarkDown文本编辑器。</li>
<li>有OS X、windows、Linux三个平台版本，是完全免费的。</li>
<li>个人平时学习的记录都是使用Typora完成的。</li>
</ul>
</blockquote>
<h2 id="Hexo"><a href="#Hexo" class="headerlink" title="Hexo"></a>Hexo</h2><blockquote>
<p>Hexo是一个快速、简洁且高效的博客框架。</p>
<p>Hexo使用MarkDown解析文章，可利用靓丽的主题生成<strong>静态网页</strong>。</p>
</blockquote>
<h2 id="Typora-Hexo"><a href="#Typora-Hexo" class="headerlink" title="Typora+Hexo"></a>Typora+Hexo</h2><blockquote>
<p>实际使用过程中，为了让自己本地的记录能够随时可以查看，参考网上教程，使用了Hexo+GitHub Pages实现静态博客功能，本地使用Typora进行编辑记录，完成后发布到GitHub。</p>
</blockquote>
<h2 id="文章中图片的一致性处理"><a href="#文章中图片的一致性处理" class="headerlink" title="文章中图片的一致性处理"></a>文章中图片的一致性处理</h2><blockquote>
<p>如果正常在本地使用typora，并使用了一些图片后，将文章上传至GitHub中，会发现在远程中图片无法显示，这是因为Hexo中的图片路径访问不到，需要对Typora进行一定处理。</p>
</blockquote>
<h3 id="Typora中图片路径"><a href="#Typora中图片路径" class="headerlink" title="Typora中图片路径"></a>Typora中图片路径</h3><img src="/imgs/testImage/image-20201128154335673.png" alt="" style="zoom:80%;" /> 

<p>本地使用Typora编辑内容时，可以设置插入图片时是否复制图片</p>
<img src="/imgs/testImage/image-20201128154530131.png" alt="" style="zoom:80%;" /> 

<ul>
<li><p>如果<strong>不复制图片</strong>，则路径为复制图片的源路径（Typora缓存路径）</p>
<img src="/imgs/testImage/image-20201128154805241.png" alt="" style="zoom:67%;" /> 
</li>
<li><p>如果设置了<strong>复制图片</strong>，则会根据设置的路径将图片复制进去，并进行引用（默认为当前文章所在位置的相对路径）</p>
<p><img src="/imgs/testImage/image-20201128155025584.png"> </p>
</li>
<li><p>如果要使用<strong>绝对路径</strong>，则会使用计算机本地的绝对路径</p>
</li>
<li><p>Typora中可以针对文章设置根目录，设置后当前文章会以根目录为准，所有资源的<strong>绝对路径</strong>可<strong>使用根目录的相对路径来表示</strong></p>
<ul>
<li><p>工具栏设置文章根目录：</p>
<img src="/imgs/testImage/image-20201128155509532.png" alt="" style="zoom:50%;" />     
</li>
<li><p>使用表头格式内容进行设置（最终都是使用该方式实现的）</p>
<img src="/imgs/testImage/image-20201128155638464.png" alt="" style="zoom:80%;" /> 

</li>
</ul>
</li>
</ul>
<h3 id="Hexo框架中图片引用路径"><a href="#Hexo框架中图片引用路径" class="headerlink" title="Hexo框架中图片引用路径"></a>Hexo框架中图片引用路径</h3><blockquote>
<p>使用Hexo博客框架时，文章中的图片路径有两种情况</p>
</blockquote>
<ul>
<li><p>Hexo中<strong>新增文章</strong>存放路径均是在<code>hexoBlog/source/_post</code> 文件夹下</p>
<p><img src="/imgs/testImage/image-20201128160004391.png"> </p>
</li>
<li><p><strong>默认配置：</strong>Hexo默认情况下，使用的图片路径为 <code>hexoBlog/sopurce/imgs</code> ，放在imgs文件夹下的图片都可以使用 <code>/imgs/.../photo.png</code> 来进行引用</p>
<p><img src="/imgs/testImage/image-20201128160117267.png"> </p>
</li>
<li><p><strong>更改默认配置：</strong>Hexo的全局配置文件 <code>_config.yml</code> 中修改设置为：</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment">#默认是false，</span></span><br><span class="line"><span class="comment">#修改为true代表每次创建文件会同时在相同位置创建一个同名文件夹</span></span><br><span class="line"><span class="comment">#同名文件夹中资源可以直接使用路径进行访问，如： photo.png</span></span><br><span class="line"><span class="attr">post_asset_floder:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="Typora和Hexo一致处理"><a href="#Typora和Hexo一致处理" class="headerlink" title="Typora和Hexo一致处理"></a>Typora和Hexo一致处理</h3><blockquote>
<p>为了让文章中的图片在本地编辑时可用，发布到远程依然可用，我们进行一些一致的设置。</p>
</blockquote>
<p>我们在Typora中设置复制图片时保存的路径，为了便于管理图片，我们使用了在 <code>HexoBlog/source/imgs</code> 下的同名文件夹来保存当前文章对应的图片信息。</p>
<ul>
<li><p>设置图片复制路径为：</p>
<img src="/imgs/testImage/image-20201128161058627.png" alt="" style="zoom:80%;" /> 

<p>这是一个绝对路径，表示本地Hexo的项目Blog项目中的source文件夹下面找到文章的同名文件夹（${filename}），所有图片放入同名文件夹中，然后在Typora中进行资源的引用。</p>
</li>
<li><p>为了引用时与Hexo引用一致，我们设置当前文章的根目录为source文件夹</p>
<img src="/imgs/testImage/image-20201128161406324.png" alt="" style="zoom:50%;" /> 

<p>设置后文章表头会出现如下信息：此处是使用的相对路径进行的设置</p>
<img src="/imgs/testImage/image-20201128155638464.png" alt="" style="zoom:100%;" /> 
</li>
<li><p>这样之后，我们在文章中复制插入图片时，图片的引用路径为：</p>
<p><img src="/imgs/testImage/image-20201128161748049.png"> </p>
<p>这个路径正是Hexo框架的默认图片引用路径，这样我们就完成了一致处理。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo使用Next主题</title>
    <url>/hexo-change-theme/</url>
    <content><![CDATA[<p>Next是一款符合程序员审美的主题，<code>Hexo</code>中切换Next主题的方式非常简单，只需要将主题文件拷贝至根目录下的<code>themes</code>文件夹中，然后修改<code>_config.yml</code>文件中的<code>theme</code>字段即可。 <a id="more"></a> </p>
<h1 id="Next主题"><a href="#Next主题" class="headerlink" title="Next主题"></a>Next主题</h1><h2 id="主题下载"><a href="#主题下载" class="headerlink" title="主题下载"></a>主题下载</h2><h3 id="Git命令行下载"><a href="#Git命令行下载" class="headerlink" title="Git命令行下载"></a>Git命令行下载</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Hexo根目录下执行命令</span><br><span class="line">$ git clone https:&#x2F;&#x2F;github.com&#x2F;theme-next&#x2F;hexo-theme-next.git themes&#x2F;next</span><br></pre></td></tr></table></figure>

<h3 id="发布网站下载"><a href="#发布网站下载" class="headerlink" title="发布网站下载"></a>发布网站下载</h3><p>在<a href="https://github.com/theme-next/hexo-theme-next/releases">Next发布网站</a>中手动下载源文件，并解压到根目录下的theme文件夹下，源文件名修改为next。</p>
<h2 id="配置新主题"><a href="#配置新主题" class="headerlink" title="配置新主题"></a>配置新主题</h2><blockquote>
<p>打开Hexo的配置文件<code>_config.yml</code>，将theme字段的值修改为next，即使用<code>theme/next</code>主题，重启后生效。</p>
</blockquote>
<p>Next主题默认风格为Muse，可以在主题配置文件中修改<code>scheme</code>字段来选择喜欢的主题风格</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="string">themes\next\_config.yml</span></span><br><span class="line"><span class="comment"># Schemes</span></span><br><span class="line"><span class="attr">scheme:</span> <span class="string">Muse</span></span><br><span class="line"><span class="comment">#scheme: Mist  #Muse的紧凑版本，整洁有序的单栏外观</span></span><br><span class="line"><span class="comment">#scheme: Pisces	#双栏Scheme，小家碧玉似的清新</span></span><br><span class="line"><span class="comment">#scheme: Gemini</span></span><br></pre></td></tr></table></figure>

<h2 id="站点优化"><a href="#站点优化" class="headerlink" title="站点优化"></a>站点优化</h2><blockquote>
<p>根目录下的<code>_config.yml</code>文件用来配置站点相关属性，可以通过修改此文件来自定义站点的内容或功能，修改后重启服务器才能看到效果。</p>
</blockquote>
<h3 id="基本信息"><a href="#基本信息" class="headerlink" title="基本信息"></a>基本信息</h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment">#_config.yml中配置相关信息</span></span><br><span class="line"><span class="comment"># 注意字段属性和值之间要有 一个空格 间隔</span></span><br><span class="line"><span class="attr">title:</span> <span class="string">Shone&#x27;s</span> <span class="string">Blog</span>	<span class="comment">#站点名称</span></span><br><span class="line"><span class="attr">description:</span> <span class="string">harry</span> <span class="string">up!</span> <span class="comment">#站点描述</span></span><br><span class="line"><span class="attr">language:</span> <span class="string">zh-CN</span>	<span class="comment">#站点使用语言为简体中文</span></span><br><span class="line"><span class="attr">author:</span> <span class="string">shone</span>	<span class="comment">#设置站点作者</span></span><br></pre></td></tr></table></figure>

<h3 id="网站Favicon"><a href="#网站Favicon" class="headerlink" title="网站Favicon"></a>网站<code>Favicon</code></h3><blockquote>
<p><code>Favicon</code>是浏览器标签左侧的图标，可以在配置文件中自定义设置图标。</p>
</blockquote>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># themes\next\_config.yml</span></span><br><span class="line"><span class="attr">favicon:</span></span><br><span class="line">  <span class="attr">small:</span> <span class="string">/images/favicon-16x16-next.png</span>  <span class="comment"># 小图标</span></span><br><span class="line">  <span class="attr">medium:</span> <span class="string">/images/favicon-32x32-next.png</span>  <span class="comment"># 大图标</span></span><br><span class="line">  <span class="attr">apple_touch_icon:</span> <span class="string">/images/apple-touch-icon-next.png</span>  <span class="comment"># 苹果图标</span></span><br><span class="line">  <span class="attr">safari_pinned_tab:</span> <span class="string">/images/logo.svg</span>  <span class="comment"># safari浏览器标签页图标</span></span><br></pre></td></tr></table></figure>

<h3 id="侧栏信息"><a href="#侧栏信息" class="headerlink" title="侧栏信息"></a>侧栏信息</h3><blockquote>
<p>侧栏信息默认停靠在左侧，且默认不开启，需要手动点击。</p>
</blockquote>
<ul>
<li><p>停靠位置：设置侧栏的位置，修改主题配置文件中 <code>sidebar.position</code> 的值</p>
<ul>
<li>left：停靠左边</li>
<li>right：停靠右边</li>
</ul>
</li>
<li><p>显示时机：修改侧栏的显示时机，修改主题配置文件中 <code>sidbar.display</code> 的值</p>
<ul>
<li>post：默认，在文件页面（拥有目录时）显示</li>
<li>always：在所有页面都显示</li>
<li>hide：所有页面都隐藏，可以手动点击展开</li>
<li>remove：完全移除</li>
</ul>
</li>
<li><p>文章目录：默认目录不全部展开，只显示第一级，可以在主题配置文件中修改设置</p>
<img src="/imgs/hexo-change-theme/image-20201216195130377.png" alt="image-20201216195130377" style="zoom:50%;" />

</li>
</ul>
<h3 id="展示头像"><a href="#展示头像" class="headerlink" title="展示头像"></a>展示头像</h3><p>修改展示头像时，需要主题配置文件中设定 <code>avatr</code> 字段的值，设置成头像的链接地址。可以是：</p>
<ol>
<li>完整的网络URI：<code>http://example.com/avatar.png</code></li>
<li>站内地址：头像放在<code>themes/next/source/images</code>目录下，使用路径<code>/images/avatar.png</code>引用 </li>
</ol>
<img src="/imgs/hexo-change-theme/image-20201216154025479.png" alt="image-20201216154025479" style="zoom:50%;" /> 

<h3 id="首页显示"><a href="#首页显示" class="headerlink" title="首页显示"></a>首页显示</h3><blockquote>
<p>next主题默认首先显示每一篇文章的全文，如果只需要显示文章摘要，可以在主题配置文件中设置</p>
</blockquote>
<p>设置**[阅读全文]**可以通过以下三种方法：</p>
<ol>
<li><p>用户可以使用<code>&lt;!-- more --&gt;</code> 标记来精确划分摘要信息，标记之前的段落将作为摘要显示在首页。</p>
<blockquote>
<p>建议使用此种方式，可以精确控制摘要内容，且Hexo中插件更好识别。</p>
</blockquote>
</li>
<li><p>如果文章的<code>Front-Matter</code>中有非空的description字段，该字段内容会作为摘要显示在首页。</p>
</li>
<li><p>自动形成摘要，在主题配置文件中添加：</p>
</li>
</ol>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># themes\next\_config.yml</span></span><br><span class="line"><span class="attr">auto_excerpt:</span></span><br><span class="line">	<span class="string">enable:true</span>	<span class="comment">#开启自动摘要提取</span></span><br><span class="line">	<span class="string">length:150</span>	<span class="comment">#设置摘要内容为150个字符</span></span><br></pre></td></tr></table></figure>

<h3 id="文章meta信息"><a href="#文章meta信息" class="headerlink" title="文章meta信息"></a>文章meta信息</h3><blockquote>
<p>默认主题配置中，打开文章时标题下方显示文章的创建时间和分类信息等，用户可以在主题配置文件中自定义设置需要显示的meta元信息</p>
</blockquote>
<p><img src="/imgs/hexo-change-theme/image-20201216154347258.png" alt="默认显示内容"></p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># themes\next\_config.yml</span></span><br><span class="line"><span class="attr">post_meta:</span></span><br><span class="line">  <span class="attr">item_text:</span> <span class="literal">true</span>  <span class="comment"># 显示文字说明</span></span><br><span class="line">  <span class="attr">created_at:</span> <span class="literal">true</span>  <span class="comment"># 显示文章创建时间</span></span><br><span class="line">  <span class="attr">updated_at:</span></span><br><span class="line">    <span class="attr">enabled:</span> <span class="literal">false</span>  <span class="comment"># 隐藏文章修改时间</span></span><br><span class="line">    <span class="attr">another_day:</span> <span class="literal">true</span>  <span class="comment"># 只有当修改时间和创建时间不是同一天的时候才显示</span></span><br><span class="line">  <span class="attr">categories:</span> <span class="literal">false</span>  <span class="comment"># 隐藏分类信息</span></span><br></pre></td></tr></table></figure>

<h3 id="文章内容"><a href="#文章内容" class="headerlink" title="文章内容"></a>文章内容</h3><ul>
<li>代码高亮设置：Next使用Tomorrow Theme作为代码高亮，有5中主题可以选择。在配置文件中的<code>hightlight_theme</code>字段设置具体值。<ul>
<li>normal：白色，默认</li>
<li>night</li>
<li>night blue</li>
<li>night bright</li>
<li>night eighties</li>
</ul>
</li>
</ul>
<h3 id="页脚信息"><a href="#页脚信息" class="headerlink" title="页脚信息"></a>页脚信息</h3><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><code>https://theme-next.iissnan.com/</code><br><code>http://yearito.cn/</code>   </p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo搭建静态博客</title>
    <url>/hexo-githubpages-blog/</url>
    <content><![CDATA[<p>[TOC]</p>
<h1 id="使用Hexo搭建静态博客"><a href="#使用Hexo搭建静态博客" class="headerlink" title="使用Hexo搭建静态博客"></a>使用Hexo搭建静态博客</h1><blockquote>
<p>是配合GitHubPages静态页面，简单静态，挂载在GitHub上即可</p>
</blockquote>
<h2 id="安装Node-js"><a href="#安装Node-js" class="headerlink" title="安装Node.js"></a>安装Node.js</h2><p>Hexo是基于node.js使用的，所有在使用之前要先安装node环境，安装成功后，在控制台使用命令查看node是否安装成功，使用<code>node -v</code>和 <code>npm -v</code> 查看node版本和npm版本。 </p>
<h2 id="添加国内镜像"><a href="#添加国内镜像" class="headerlink" title="添加国内镜像"></a>添加国内镜像</h2><p>node.js源都在国外，使用过程中可能速度很慢，会安装失败，可以挂载vpn进行使用，或者使用国内镜像来安装。 <code>npm config set registry https://registry.npm.taobao.org</code> 可添加阿里云镜像。</p>
<h2 id="注册GitHub账号"><a href="#注册GitHub账号" class="headerlink" title="注册GitHub账号"></a>注册GitHub账号</h2><p>因为是要借用GitHub的仓库功能来实现静态页面的显示，首先要有一个GitHub账号，没有的话就去 <a href="https://github.com/">GitHub</a> 注册一个账号。</p>
<h2 id="创建GitHub仓库"><a href="#创建GitHub仓库" class="headerlink" title="创建GitHub仓库"></a>创建GitHub仓库</h2><p>登录自己的GitHub账号后，如果使用Hexo，则要创建一个名称有格式要求的仓库进行使用</p>
<ul>
<li><p>点击新建一个仓库，名称使用 <code>[yournamer].github.io</code> ，初始化一个readme完成仓库的创建</p>
<img src="/imgs/assets/image-20201107102323520.png" alt="image-20201107102323520" style="zoom:80%;" />
</li>
<li><p>之后进入当前仓库的 <code>Setting</code> 页面，找到 <code>GitHub Pages</code> 切换一个主题，完成后点击链接查看</p>
<img src="/imgs/assets/image-20201107102217586.png" alt="image-20201107102217586" style="zoom:80%;" />
</li>
<li><p>出现自己仓库的readme页面信息后表示创建成功】</p>
<img src="/imgs/assets/image-20201107102345187.png" alt="image-20201107102345187" style="zoom:80%;" />

</li>
</ul>
<h2 id="安装Git"><a href="#安装Git" class="headerlink" title="安装Git"></a>安装Git</h2><p>为了能够在本地与GitHub更好的进行文件资源的上传和下载，使用Git软件进行操作，下载安装Git后，在控制台使用命令查看是否安装成功， <code>git --version</code> 查看git安装版本号。</p>
<blockquote>
<p>git安装完成后，要自报家门，并且要与自己的github仓库进行关联，使用密钥进行认证</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">//①自报家门，本地Git关联名称、邮箱信息</span><br><span class="line">git config --global user.name <span class="string">&quot;yourname&quot;</span></span><br><span class="line">git config --global user.email <span class="string">&quot;111100@qq.com&quot;</span></span><br><span class="line">//②本地生成密钥 SSH Key</span><br><span class="line">ssh-keygen -t rsa -C <span class="string">&quot;111100@qq.com&quot;</span></span><br><span class="line">//③GitHub中的设置页面中，找到SSH and GPG keys，新增一个SSH</span><br><span class="line">//④查看本地生成的密钥对应的公钥</span><br><span class="line">cat ~/.ssh/id_rsa.pub</span><br><span class="line">//⑤将本地公钥内容复制到GitHub的SSH框中，点击确定保存</span><br><span class="line">//⑥本地Git使用命令查看是否与远程连接成功</span><br><span class="line">ssh -T git@github.com</span><br><span class="line">如果出现了 Hi...  你的GitHub账户名称的内容，说明链接成功</span><br></pre></td></tr></table></figure>

<img src="/imgs/assets/image-20201107104256334.png" alt="image-20201107104256334" style="zoom:80%;" />

<h2 id="安装Hexo"><a href="#安装Hexo" class="headerlink" title="安装Hexo"></a>安装Hexo</h2><p>本地node环境准备好后，进行Hexo的安装，安装过程中使用Git命令工具。</p>
<ol>
<li>首先，找一个本地存放Hexo博客文件的位置，如 <code>D:\Blog</code> </li>
<li>进入到目录中，右键选择 <code>Git Bash Here</code> 打开Git命令窗口 </li>
<li>使用命令 <code>npm i hexo-cli -g</code> 安装Heox，安装完成后使用 <code>hexo -v</code> 查看是否成功</li>
</ol>
<h2 id="初始化本地blog"><a href="#初始化本地blog" class="headerlink" title="初始化本地blog"></a>初始化本地blog</h2><p>安装Hexo后，在同目录下初始化博客网站文件，并进行本地运行效果预览。</p>
<ol>
<li><p>使用命令 <code>hexo init</code> 初始化hexo文件夹</p>
<blockquote>
<p>初始化完成后，本地有了hexo的目录，之后的操作都在根目录下的Git命令窗口中</p>
<p>初始化时可能会报错，要注意是否使用了管理员权限进行操作</p>
</blockquote>
</li>
<li><p>使用命令 <code>npm install</code> 安装必备的组件</p>
</li>
<li><p>使用命令 <code>hexo g</code> 生成静态网页</p>
</li>
<li><p>使用 <code>hexo s</code> 打开本地服务器，开启后可在浏览器 <code>localhost:4000/</code> 查看本地博客效果</p>
</li>
<li><p>查看完后，使用 <code>ctrl + c</code> 退出服务</p>
</li>
</ol>
<h2 id="连接本地和远程GitHub"><a href="#连接本地和远程GitHub" class="headerlink" title="连接本地和远程GitHub"></a>连接本地和远程GitHub</h2><p>确保本地Git已经和远程GitHub仓库链接成功，打开Hexo根目录下的 <code>_config.yml</code> 文件，修改内容，绑定GitHub指定仓库</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="string">//修改最后一行的配置为：表示和[gitHubName].github.io</span> <span class="string">这个新增仓库进行了绑定；[gitHubName]是GitHub名称；master代表主分支，这里也要对应</span></span><br><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repository:</span> <span class="string">https://github.com/[gitHubName]/[gitHubName].github.io</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">master</span></span><br></pre></td></tr></table></figure>

<h2 id="开放的博客页面"><a href="#开放的博客页面" class="headerlink" title="开放的博客页面"></a>开放的博客页面</h2><p>本地和远程指定仓库绑定后，可以将本地文件内容部署到远程，并通过Pages进行网络访问</p>
<ul>
<li><p>安装Git部署插件 <code>npm install hexo-deployer-git --save</code> </p>
</li>
<li><p>使用命令 <code>hexo clean</code>  <code>hexo g</code>  <code>hexo d</code> </p>
<blockquote>
<p> 其中<code>hexo d</code>就是进行网站的部署</p>
</blockquote>
</li>
<li><p>访问 <code>[githubname.github.io]</code> 查看部署的网站内容</p>
</li>
</ul>
<h2 id="开始写一篇博客吧"><a href="#开始写一篇博客吧" class="headerlink" title="开始写一篇博客吧"></a>开始写一篇博客吧</h2><p>写一篇新的博客时，在Hexo博客根目录下，使用 <code>git bash</code> 打开Git命令界面，在界面操作</p>
<ul>
<li><p><code>hexo new post &quot;title&quot;</code> 新增一篇名为title的博客，文件格式为 <code>title.md</code> </p>
<blockquote>
<p>本地文章生成在 <code>D:\Blog\source\_posts</code> 目录下，资源文件都会在source文件夹下</p>
</blockquote>
</li>
<li><p>可以使用本地markdown编辑器进行博客内容的书写</p>
</li>
</ul>
<h2 id="博客发布"><a href="#博客发布" class="headerlink" title="博客发布"></a>博客发布</h2><ul>
<li> 使用markdown操作完文件后，使用 <code>hexo g</code> 生成静态网页</li>
<li>使用 <code>hexo s</code> 可以开启本地服务器，在 <code>localhost:4000/</code> 预览效果</li>
<li>使用 <code>hexo d</code> 上传到GitHub上，并部署博客网站，此时可以使用 <code>..github.io</code> 网络地址进行访问</li>
</ul>
<h2 id="绑定域名"><a href="#绑定域名" class="headerlink" title="绑定域名"></a>绑定域名</h2><p>如果觉得GitHub给的域名不够个性，可以自己购买一个域名，然后绑定到GitHubPages中来，之后可以直接使用自己的域名进行博客页面的访问。</p>
<ol>
<li><p>购买域名并进行域名相关的设置</p>
</li>
<li><p>在GitHub对应仓库中，添加域名，保存</p>
<img src="/imgs/assets/image-20201107111256123.png" alt="image-20201107111256123" style="zoom:80%;" />
</li>
<li><p>设置完成后，本地根目录中出现 CNAME 的文件，内容为域名地址，再使用 <code>hexo g</code> <code>hexo d</code> 部署。</p>
</li>
</ol>
<h2 id="博客个性化设置"><a href="#博客个性化设置" class="headerlink" title="博客个性化设置"></a>博客个性化设置</h2><p><a href="https://github.com/blinkfox/hexo-theme-matery/blob/develop/README_CN.md">一个开源的Hexo响应式主题-内容很丰富</a>  </p>
<h2 id="参考文章"><a href="#参考文章" class="headerlink" title="参考文章"></a>参考文章</h2><p><a href="https://zhuanlan.zhihu.com/p/35668237">知乎-韦阳，教你个性化使用Hexo搭建博客</a> </p>
<p><a href="https://zhuanlan.zhihu.com/p/26625249">知乎专栏-个性化操作Hexo博客</a> </p>
<p><a href="https://cloud.tencent.com/developer/article/1520557">使用Hexo搭建博客并进行 SEO</a> </p>
<h2 id="使用过程常见问题"><a href="#使用过程常见问题" class="headerlink" title="使用过程常见问题"></a>使用过程常见问题</h2><p>① <code>hexo init blog</code> 时报错</p>
<p><img src="/imgs/assets/image-20201106211559408.png" alt="image-20201106211559408"></p>
<p><strong>解决办法：</strong>将当前文件夹下已经下载的文件删除掉，使用管理员权限重新执行命令即可</p>
<p>② <code>hexo d</code> 时报错</p>
<blockquote>
<p>使用 <code>ssh -T git@github.com</code> 是没问题的</p>
</blockquote>
<img src="/imgs/assets/image-20201107163545472.png" alt="image-20201107163545472" style="zoom:80%;" />

<p><strong>解决办法：</strong> 一般就是git协议的问题，可以将deploy的地址的http协议换成 ssh协议的</p>
<img src="/imgs/assets/image-20201107163821751.png" alt="image-20201107163821751" style="zoom:80%;" />

<p>③ 使用不蒜子 进行网络访问计数时，本地服务器运行预览时，访问次数数量巨大</p>
<blockquote>
<p>是不蒜子计数的问题，实际部署上去就可以了  <a href="http://busuanzi.ibruce.info/">不蒜子使用教程</a> </p>
</blockquote>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>多线程实现内容交替输出</title>
    <url>/multithreading/</url>
    <content><![CDATA[<h1 id="Java多线程并发"><a href="#Java多线程并发" class="headerlink" title="Java多线程并发"></a>Java多线程并发</h1><img src="/imgs/multithreading/image-20201204193843419.png" alt="image-20201204193843419" style="zoom:67%;" /> 

<a id="more"></a>

<h2 id="多线程基础"><a href="#多线程基础" class="headerlink" title="多线程基础"></a>多线程基础</h2><h3 id="基础概念"><a href="#基础概念" class="headerlink" title="基础概念"></a>基础概念</h3><p><strong>进程：</strong></p>
<p><strong>线程：</strong></p>
<ul>
<li>线程是操作系统能够进行运算调度的最小单位，包含在进程之中，是进程中的实际运作单位。</li>
<li>一条线程指的是进程中一个单一顺序的控制流，一个进程中可以并发多个线程，每条线程并行执行不同的任务。</li>
</ul>
<h3 id="创建线程的方式"><a href="#创建线程的方式" class="headerlink" title="创建线程的方式"></a>创建线程的方式</h3><p><strong>继承Thread类：</strong></p>
<p><strong>实现Runnable接口：</strong></p>
<p><strong>基于Callable和Future接口：</strong></p>
<p><strong>使用线程池：</strong></p>
<h3 id="线程的几种状态"><a href="#线程的几种状态" class="headerlink" title="线程的几种状态"></a>线程的几种状态</h3><blockquote>
<p>新增、就绪、运行、阻塞、终止</p>
</blockquote>
<img src="/imgs/multithreading/image-20201204194410261.png" alt="image-20201204194410261" style="zoom:67%;" /> 

<h3 id="线程执行机制"><a href="#线程执行机制" class="headerlink" title="线程执行机制"></a>线程执行机制</h3><blockquote>
<p>JVM中一个应用可以有多个线程并行执行，线程被一对一映射为服务所在操作系统线程，调度在可用的CPU上执行，启动时会创建一个操作系统线程；当线程终止时，这个操作系统线程也会被回收。</p>
</blockquote>
<img src="/imgs/multithreading/image-20201204194643541.png" alt="image-20201204194643541" style="zoom:67%;" /> 

<h3 id="线程运行内存模型"><a href="#线程运行内存模型" class="headerlink" title="线程运行内存模型"></a>线程运行内存模型</h3><blockquote>
<p>Java虚拟机启动运行时，会创建多个线程，数据区中有的模块是线程共享的，有的是线程私有的</p>
<ul>
<li>线程共享的有：堆（Heap）、方法区（元空间）</li>
<li>线程私有的是：程序计数器、虚拟机栈、本地方法栈</li>
</ul>
<p>单个CPU在特定时刻只能执行一个线程，所以多线程通过几块空间的使用，然后不断的争抢CPU的执行时间段。</p>
</blockquote>
<img src="/imgs/multithreading/image-20201204194932940.png" alt="image-20201204194932940" style="zoom:67%;" /> 

<h3 id="其他线程概念"><a href="#其他线程概念" class="headerlink" title="其他线程概念"></a>其他线程概念</h3><ol>
<li><strong>线程优先级:</strong> 线程调度器倾向执行线程优先级高的线程，线程优先级高说明获取CPU资源的概率高，或者获取的执行时间分片多，即被执行的概率高，但优先级低的不代表一定最后执行。</li>
<li><strong>守护线程：</strong> 守护线程是支持辅助型线程，主要在程序中起到调度和支持性作用，当JVM中非守护线程全部结束，守护线程也就会结束。如JVM中的<code>GC</code>线程就是一个守护线程。</li>
<li><strong>线程加入：</strong> 线程A中，执行线程B的加入方法，那么A线程就会等待线程B执行完毕再返回继续执行。可使用<code>Thread.join()</code>加入一个线程。</li>
<li><strong>本地线程：</strong> <code>ThreadLocal</code>也叫做线程本地变量，是变量在每个线程中创建的副本变量，每个线程可以访问自己内部的副本变量，线程之间不相互影响。 </li>
</ol>
<h2 id="多线程安全"><a href="#多线程安全" class="headerlink" title="多线程安全"></a>多线程安全</h2><h3 id="同步控制"><a href="#同步控制" class="headerlink" title="同步控制"></a>同步控制</h3><h3 id="加锁机制"><a href="#加锁机制" class="headerlink" title="加锁机制"></a>加锁机制</h3><h1 id="多线程交替打印ABCDEABCDEABCDE的实现方法"><a href="#多线程交替打印ABCDEABCDEABCDE的实现方法" class="headerlink" title="多线程交替打印ABCDEABCDEABCDE的实现方法"></a>多线程交替打印<code>ABCDEABCDEABCDE</code>的实现方法</h1><h2 id="Synchronized同步方法"><a href="#Synchronized同步方法" class="headerlink" title="Synchronized同步方法"></a>Synchronized同步方法</h2><h2 id="ReentrantLock同步方法"><a href="#ReentrantLock同步方法" class="headerlink" title="ReentrantLock同步方法"></a><code>ReentrantLock</code>同步方法</h2><h2 id="Lock-Condition方式"><a href="#Lock-Condition方式" class="headerlink" title="Lock+Condition方式"></a>Lock+Condition方式</h2><h2 id="Semaphore信号量方式"><a href="#Semaphore信号量方式" class="headerlink" title="Semaphore信号量方式"></a>Semaphore信号量方式</h2>]]></content>
  </entry>
  <entry>
    <title>Hexo使用Next主题的优化策略</title>
    <url>/hexo-next-enhance/</url>
    <content><![CDATA[<p>Next主题进阶使用学习。</p>
<a id="more"></a> 



<h2 id="1-设置头像边框为圆形和旋转特效"><a href="#1-设置头像边框为圆形和旋转特效" class="headerlink" title="1.设置头像边框为圆形和旋转特效"></a>1.设置头像边框为圆形和旋转特效</h2><h3 id="自己设置："><a href="#自己设置：" class="headerlink" title="自己设置："></a>自己设置：</h3><p><img src="/imgs/Hexo-Next-Enhance/image-20210319204140943.png" alt="image-20210319204140943"></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319205220310.png" alt="image-20210319205220310"></p>
<p>系统自带：next已经内置了可以调节的功能，只需要将开关打开即可</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319205633875.png" alt="image-20210319205633875"></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319205653365.png" alt="image-20210319205653365"></p>
<h2 id="2-本地搜索"><a href="#2-本地搜索" class="headerlink" title="2.本地搜索"></a>2.本地搜索</h2><p>根目录下使用git命令：<code>npm install hexo-generator-searchdb --save</code> </p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319210847331.png" alt="image-20210319210847331"></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319210928934.png" alt="image-20210319210928934"></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319211115663.png" alt="image-20210319211115663"></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319211136014.png" alt="image-20210319211136014"></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319211251125.png" alt="image-20210319211251125"></p>
<h2 id="3-博文置顶"><a href="#3-博文置顶" class="headerlink" title="3.博文置顶"></a>3.博文置顶</h2><p>使用文章的top属性，设置值越大，权重越大  top :100</p>
<h2 id="4-统计文章字数"><a href="#4-统计文章字数" class="headerlink" title="4.统计文章字数"></a>4.统计文章字数</h2><p>根目录下安装：<code>npm i --save hexo-wordcount</code></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319214610604.png" alt="image-20210319214610604"></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319214636660.png" alt="image-20210319214636660"></p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319215657516.png" alt="image-20210319215657516"></p>
<p>Hexo 配置文件</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319223907209.png" alt="image-20210319223907209"></p>
<p>Next配置文件</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210319223843925.png" alt="image-20210319223843925"></p>
<h2 id="文章结尾添加-“本文结束”-标记"><a href="#文章结尾添加-“本文结束”-标记" class="headerlink" title="文章结尾添加 “本文结束” 标记"></a>文章结尾添加 “本文结束” 标记</h2><p>新增文件，添加内容</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210320105817230.png" alt="image-20210320105817230"> </p>
<p>将新文件关联到post文件中</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210320091723707.png" alt="image-20210320091723707"></p>
<p>在主题配置文件中开启</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210320092143130.png" alt="image-20210320092143130"></p>
<h2 id="修改访问路径"><a href="#修改访问路径" class="headerlink" title="修改访问路径"></a>修改访问路径</h2><blockquote>
<p>Hexo默认访问路径是以日期为路径，可以在显示时将日期隐藏</p>
</blockquote>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210320093135394.png" alt="image-20210320093135394"></p>
<h2 id="文章加密访问"><a href="#文章加密访问" class="headerlink" title="文章加密访问"></a>文章加密访问</h2><p><img src="/imgs/Hexo-Next-Enhance/image-20210320093854335.png" alt="image-20210320093854335"></p>
<h2 id="设置个人社交链接"><a href="#设置个人社交链接" class="headerlink" title="设置个人社交链接"></a>设置个人社交链接</h2><p><img src="/imgs/Hexo-Next-Enhance/image-20210320100414940.png" alt="image-20210320100414940"></p>
<h2 id="Hexo博客live2d看板娘"><a href="#Hexo博客live2d看板娘" class="headerlink" title="Hexo博客live2d看板娘"></a>Hexo博客live2d看板娘</h2><p>执行命令安装：</p>
<p><code>npm install --save hexo-helper-live2d </code></p>
<p><code>npm install --save live2d-widget-model-tororo</code></p>
<p>添加设置</p>
<p>这个要在站点配置文件中设置！！！</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210320111131765.png" alt="image-20210320111131765"></p>
<p>是个这：</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210320113659962.png" alt="image-20210320113659962"></p>
<h2 id="文章底部版权信息"><a href="#文章底部版权信息" class="headerlink" title="文章底部版权信息"></a>文章底部版权信息</h2><p><img src="/imgs/Hexo-Next-Enhance/image-20210320112122199.png" alt="image-20210320112122199"></p>
<p>要配合开启：</p>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210320113209460.png" alt="image-20210320113209460"></p>
<h2 id="设置访问量"><a href="#设置访问量" class="headerlink" title="设置访问量"></a>设置访问量</h2><p>？？？</p>
<h2 id="左侧标题栏设置"><a href="#左侧标题栏设置" class="headerlink" title="左侧标题栏设置"></a>左侧标题栏设置</h2><h2 id="博客底部设置"><a href="#博客底部设置" class="headerlink" title="博客底部设置"></a>博客底部设置</h2><blockquote>
<p>显示时间、隐藏Hexo</p>
</blockquote>
<p><img src="/imgs/Hexo-Next-Enhance/image-20210320105545766.png" alt="image-20210320105545766"></p>
<h2 id="首页隐藏指定文章"><a href="#首页隐藏指定文章" class="headerlink" title="首页隐藏指定文章"></a>首页隐藏指定文章</h2><p><a href="https://www.jianshu.com/p/79fe9fb9dfa0">https://www.jianshu.com/p/79fe9fb9dfa0</a></p>
<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a href="https://www.jianshu.com/p/efbeddc5eb19">https://www.jianshu.com/p/efbeddc5eb19</a></p>
<p><a href="https://blog.csdn.net/zgcr654321/article/details/86751765">https://blog.csdn.net/zgcr654321/article/details/86751765</a></p>
<p><a href="https://io-oi.me/tech/hexo-next-optimization/">https://io-oi.me/tech/hexo-next-optimization/</a></p>
<p><a href="https://blog.csdn.net/qq_32767041/article/details/103284412">https://blog.csdn.net/qq_32767041/article/details/103284412</a></p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
  </entry>
  <entry>
    <title>MySQL学习笔记</title>
    <url>/mysql-xue-xi-bi-ji/</url>
    <content><![CDATA[<p>[TOC]</p>
<h1 id="MySQL学习笔记"><a href="#MySQL学习笔记" class="headerlink" title="MySQL学习笔记"></a>MySQL学习笔记</h1><h2 id="1-MySQL基础知识"><a href="#1-MySQL基础知识" class="headerlink" title="1. MySQL基础知识"></a>1. MySQL基础知识</h2><blockquote>
<p>参考文章： <a href="https://mp.weixin.qq.com/s?__biz=Mzg2MjEwMjI1Mg==&mid=2247503444&idx=3&sn=1e307a247838092dd22c4ccfcde71193&chksm=ce0e79d7f979f0c14c3e1e31e6ea951e5c71b13489257cd7df1607187a675860b120964d689c&mpshare=1&scene=24&srcid=1014ASyBj2RHmR9v2JM1GuDk&sharer_sharetime=1602682491574&sharer_shareid=c90d89447bde2426811e6f3556bd276d&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=AZIVNnQgDS02YP26FewfMQA=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">mysql零散的基础知识</a> </p>
</blockquote>
<h3 id="1-1-SQL命令"><a href="#1-1-SQL命令" class="headerlink" title="1.1 SQL命令"></a>1.1 SQL命令</h3><blockquote>
<p>数据库SQL命令可以分为四组：DDL、DML、DCL和TCL</p>
</blockquote>
<img src="assets/SQL命令结构图.png" alt="image-20201107212233910" style="zoom:80%;" />

<p><strong>DDL(Data Definition Language)：</strong>  </p>
<blockquote>
<p>数据定义语言，处理数据库schemas和描述数据应如何驻留在数据库中</p>
</blockquote>
<p><strong>DML(Data Manipulation Language)：</strong> </p>
<blockquote>
<p>数据操纵语言，包括常见的SQL语句，用于存储、修改、检索和删除数据库中的数据</p>
</blockquote>
<p><strong>DCL(Data Control Language)：</strong> </p>
<blockquote>
<p>数据控制语言，包含如Grant之类的命令，主要涉及数据库系统的权限等控件</p>
</blockquote>
<p><strong>TCL(Transaction Control Language)：</strong> </p>
<blockquote>
<p>事务控制语言，用于处理数据库中的事务</p>
</blockquote>
<h3 id="1-2-数据库范式"><a href="#1-2-数据库范式" class="headerlink" title="1.2 数据库范式"></a>1.2 数据库范式</h3><blockquote>
<p>现在数据库范式最多满足3NF，普遍认为范式过高虽然对数据关系有更好的约束性，但也导致数据关系表增加而令数据库IO更易繁忙</p>
</blockquote>
<p><strong>第一范式：</strong></p>
<blockquote>
<p>第一范式要求数据库中的所有字段列都是单一属性，不可再分的。</p>
</blockquote>
<p><strong>第二范式：</strong> </p>
<blockquote>
<p>在满足第一范式的基础上，数据库中的表不存在非关键字段对任意关键字段的部分依赖</p>
</blockquote>
<p><strong>第三范式：</strong> </p>
<blockquote>
<p>在满足第二范式的基础上，所有的非主键列之间不存在传递依赖</p>
</blockquote>
<p>数据库设计时，遵循范式和反范式一直都是一个颇受争议的问题。遵循范式对数据关系有更好的约束性，并且减少数据冗余，可以更好的保证数据一致性。而反范式则是为了获得更好的性能。因此实际中要选择适合自己业务场景的。</p>
<p>反范式设计时，要考虑到关系表之间的<strong>插入异常</strong>、<strong>更新异常</strong>和<strong>删除异常</strong>。</p>
<h3 id="1-3-横表纵表"><a href="#1-3-横表纵表" class="headerlink" title="1.3 横表纵表"></a>1.3 横表纵表</h3><h3 id="1-4-数据类型"><a href="#1-4-数据类型" class="headerlink" title="1.4 数据类型"></a>1.4 数据类型</h3><p><strong>整数类型(byte)：</strong> BIT、BOOL、TINY INT(1)、SMALL INT(2)、 MEDIUM INT(3)、INT(4)、BIG INT(8)</p>
<p><strong>浮点数类型：</strong> FLOAT(4)、DOUBL(8)、DECIMAL</p>
<p><strong>字符串类型：</strong> CHAR、VARCHAR、TINY TEXT、 TEXT、MEDIUM TEXT、 LONG TEXT、TINY BLOB、BLOB、MEDIUM BLOB、LONG BLOB</p>
<p><strong>日期类型：</strong> DATE、TIME、YEAR、DATETIME、TIMESTAMP</p>
<p>其他数据类型：ENUM、BINARY</p>
<blockquote>
<p><strong>CHAR和VARCHAR的区别什么？</strong></p>
</blockquote>
<ul>
<li>CHAR(n)和VARCHAR(n)的长度n都代表存放字符的个数，超过设定长度n的字符串会被截断 </li>
<li>CHAR是固定长度，VARCHAR是可变长度</li>
<li>CHAR无论是否存满都会占用指定长度，不足的补空；VARCHAR只占用实际字符的长度，超出会截断</li>
<li>CHAR的长度上限是255，VARCHAR是65535</li>
</ul>
<blockquote>
<p><strong>BLOB和TEXT的区别是什么？</strong></p>
</blockquote>
<ul>
<li>BLOB是一个<strong>二进制对象</strong>，可以容纳可变数量的数据</li>
<li>TEXT是一个不区分大小写的BLOB，保存的是<strong>字符数据</strong></li>
</ul>
<h2 id="2-MySQL架构"><a href="#2-MySQL架构" class="headerlink" title="2. MySQL架构"></a>2. MySQL架构</h2><blockquote>
<p>待完成。   <a href="https://mp.weixin.qq.com/s?__biz=MzI1MTIzMzI2MA==&mid=2650565395&idx=1&sn=e9c779d36d944fa13703d9b8c2fd88b2&chksm=f1fedd90c6895486fb19073d3b1016d8ec4ca9cc13a5a87c198164a4ca683d193fd687e95840&scene=0&xtrack=1&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=Af5ZwF/scurohiRR5ZXz03s=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">为什么别人能用好MySQL？</a>  </p>
<p>MySQL的逻辑架构可以分为四层，包括连接层、服务层、引擎曾和存储层。（InnoDB引擎下）</p>
<ul>
<li>连接层：负责处理客户端的连接以及权限的认证</li>
<li>服务层：定义有许多不同的模块，包括权限判断，SQL接口，SQL解析，SQL分析优化，缓存查询的处理以及部分内置函数执行等。MySQL的查询语句在读物曾进行解析、优化、缓存以及内置函数的实现和存储。</li>
<li>引擎层：负责MySQL中数据的存储和提取。MySQL中的服务器层不管理事务，事务是由存储引擎实现的，使用最广泛的存储引擎为InnoDB，其他不支持事务。</li>
<li>存储层：负责将数据存储到设备的文件系统中。</li>
</ul>
</blockquote>
<img src="assets/image-20201125200416543.png" alt="image-20201125200416543" style="zoom:80%;" /> 



<h2 id="3-MySQL存储引擎"><a href="#3-MySQL存储引擎" class="headerlink" title="3. MySQL存储引擎"></a>3. MySQL存储引擎</h2><blockquote>
<p>参考文章： <a href="https://mp.weixin.qq.com/s?__biz=Mzg2MjEwMjI1Mg==&mid=2247492928&idx=3&sn=09b88a89ef9c742d7a9d2d9d0eb93eba&chksm=ce0e52c3f979dbd58f039f197c3b88972addd72b4ee72df23d4bb5e8d6ca66b684fbb692c07f&scene=0&xtrack=1&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=AfLZNfcbdI+feRZ+DkWDxiE=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">MySQL存储引擎 - InnoDB-一棵树可以放多少数据</a>  </p>
</blockquote>
<blockquote>
<p>存储引擎是MySQL的组件，用于处理不同<strong>表类型</strong>的SQL操作。不同的存储引擎提供不同的存储机制、索引技巧、锁定水平等功能。  存储引擎是针对数据表的，每个表创建时不指定则会使用默认引擎。</p>
</blockquote>
<h3 id="3-1-存储引擎的设置和修改"><a href="#3-1-存储引擎的设置和修改" class="headerlink" title="3.1 存储引擎的设置和修改"></a>3.1 存储引擎的设置和修改</h3><p><strong>查看存储引擎：</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 查看支持的存储引擎</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">ENGINES</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 查看默认存储引擎</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">VARIABLES</span> <span class="keyword">LIKE</span> <span class="string">&#x27;storage_engine&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">--查看具体某一个表所使用的存储引擎，这个默认存储引擎被修改了！</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">create</span> <span class="keyword">table</span> [tablename]</span><br><span class="line"></span><br><span class="line"><span class="comment">--准确查看某个数据库中的某一表所使用的存储引擎</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">table</span> <span class="keyword">status</span> <span class="keyword">like</span> <span class="string">&#x27;tablename&#x27;</span></span><br><span class="line"><span class="keyword">show</span> <span class="keyword">table</span> <span class="keyword">status</span> <span class="keyword">from</span> <span class="keyword">database</span> <span class="keyword">where</span> <span class="keyword">name</span> = <span class="string">&#x27;tablename&#x27;</span></span><br></pre></td></tr></table></figure>

<p><strong>设置存储引擎：</strong></p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">-- 建表时指定存储引擎。默认的就是INNODB，不需要设置</span></span><br><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> [tablename] (<span class="built_in">int</span> i...) <span class="keyword">ENGINE</span>=CSV</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 修改存储引擎</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">TABLE</span> [tablename] <span class="keyword">ENGINE</span>=<span class="keyword">InnoDB</span></span><br><span class="line"></span><br><span class="line"><span class="comment">-- 修改默认存储引擎，也可以在配置文件my.cnf中修改默认引擎</span></span><br><span class="line"><span class="keyword">SET</span> default_storage_engine=NDBCLUSTER</span><br></pre></td></tr></table></figure>

<h3 id="3-2-常见存储引擎"><a href="#3-2-常见存储引擎" class="headerlink" title="3.2 常见存储引擎"></a>3.2 常见存储引擎</h3><blockquote>
<p>MySQL常见的存储引擎有 InnoDB、MyISAM、Memory、NDB，其中InnoDB是默认引擎。</p>
</blockquote>
<p><strong>InnoDB存储引擎</strong> </p>
<ul>
<li>InnoDB支持事务，面向在线事务处理(OLTP)的应用。<strong>这也是MySQL将默认引擎改为InnoDB的重要原因之一。</strong></li>
<li>InnoDB支持外键，MyISAM不支持，对有外键的InnoDB表转成MyISAM会失败。</li>
<li>InnoDB是聚簇索引，数据文件放在主键索引的叶子节点上，因此必须要有主键，主键索引效率高，使用辅助索引需要进行主键索引的回表查询（覆盖索引除外）。</li>
<li>InnoDB支持最小锁粒度是行锁，数据并发性能更好。<strong>这也是改用InnoDB引擎的重要原因之一。</strong></li>
<li>InnoDB不保存表的具体行数，执行 <code>select count(1) from [tablename]</code> 时需要扫描全表；而MyISAM有数据行计数变量，速度更快。</li>
<li>MySQL5.5.8版本之后，将默认存储引擎改成InnoDB。</li>
</ul>
<p><strong>MyISAM存储引擎</strong> </p>
<ul>
<li>MyISAM引擎不支持事务，主要面向一些OLAP数据库应用。</li>
<li>MyISAM引擎的索引与InnoDB不同的是，其索引与实际数据是分离的，索引中叶子节点存放的是具体数据的地址，是非聚集索引，主键索引和辅助索引相互独立，叶子节点中都存放了数据文件的指针。</li>
<li>MyISAM支持的最小锁粒度是表锁，一个更新语句会锁定整张表，并发性能低，安全性高。</li>
<li>MyISAM中保存了表的具体行数，查询数量时会直接读取计数变量返回，速度很快。</li>
<li>MyISAM引擎支持全文索引，InnoDB在5.6后才加入了全文索引。</li>
<li>MyISAM引擎更注重性能，速度较快一些。</li>
</ul>
<p><strong>Memory存储引擎</strong> </p>
<ul>
<li>Memory存储引擎将表中的数据放在内存中，速度更快，但是不支持持久化。</li>
</ul>
<h2 id="4-MySQL索引"><a href="#4-MySQL索引" class="headerlink" title="4. MySQL索引"></a>4. MySQL索引</h2><blockquote>
<p>参考文章： <a href="https://mp.weixin.qq.com/s?__biz=MzAwNDA2OTM1Ng==&mid=2453147214&idx=1&sn=d6ef2e8b8c139db9dc302753050f535e&chksm=8cfd2ccdbb8aa5db91198f30c6968b1ec92525887627bd7cae10a713ab8f4bf06365c3c17223&mpshare=1&scene=24&srcid=1022j7TO5jHev6IPdSbLZ79i&sharer_sharetime=1603340755611&sharer_shareid=c90d89447bde2426811e6f3556bd276d&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=AbAkE7xGgvGeXvDTs2+UCUY=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">一口气搞懂MySQL索引所有知识点</a> </p>
</blockquote>
<h3 id="4-1-索引的介绍"><a href="#4-1-索引的介绍" class="headerlink" title="4.1 索引的介绍"></a>4.1 索引的介绍</h3><blockquote>
<p>索引是帮助MySQL高效获取数据的数据机构，类似一本书的目录索引。</p>
</blockquote>
<p>通常所说的索引，有聚集索引、覆盖索引、组合索引、前缀索引、唯一索引等，默认都是使用B+树的结构组织索引。</p>
<p><strong>使用索引的好处和坏处</strong></p>
<p><strong>好处：</strong> </p>
<ul>
<li>索引可以提高数据检索的效率，降低数据库的<strong>IO</strong>成本</li>
<li>通过索引列对数据进行排序，降低了数据排序对<strong>CPU</strong>的消耗<ul>
<li>索引列会自动排序，包括单列索引和组合索引</li>
<li>如果按照索引列进行排序，order by 语句的效率会非常高</li>
</ul>
</li>
</ul>
<p><strong>坏处：</strong> </p>
<ul>
<li>索引会占据磁盘空间</li>
<li>索引虽然能够提高数据查询效率，但是会降低数据更新的效率，因为每次增删改数据操作是，都要保存或更新相应的索引文件。</li>
</ul>
<h3 id="4-2-索引的类型"><a href="#4-2-索引的类型" class="headerlink" title="4.2 索引的类型"></a>4.2 索引的类型</h3><ul>
<li><strong>唯一索引：</strong>索引列中的值必须是唯一的额，但是允许为null值</li>
<li><strong>主键索引：</strong>特殊的唯一索引，值唯一且不允许为null值</li>
<li><strong>普通索引：</strong>MySQL中基本索引类型，允许列值重复，也允许为null</li>
<li><strong>全文索引：</strong>只能在CHAR、VARCHAR、TEXT类型字段上创建全文索引。当字段长度比较大时，普通索引中使用like进行模糊查询效率比较低，可以使用全文索引 </li>
<li><strong>空间索引：</strong>5.7后支持全文索引，支持OpenGIS集合数据模型</li>
<li><strong>前缀索引：</strong>在CHAR、VARCHAR、TEXT类型的列上创建索引时，可以指定索引列的长度，即列内容的前缀进行索引。</li>
</ul>
<p>按照索引列的数量分类</p>
<ul>
<li><strong>单列索引</strong></li>
<li><strong>组合索引：</strong>组合索引使用时要遵循最左前缀匹配元组，条件允许的情况下使用组合索引替代多个单列索引</li>
</ul>
<h3 id="4-3-索引的数据结构"><a href="#4-3-索引的数据结构" class="headerlink" title="4.3 索引的数据结构"></a>4.3 索引的数据结构</h3><p><strong>Hash表：</strong></p>
<p>Hash表在等值查询时效率很高，但是不支持范围快速查找，范围查找时还是只能通过扫描全表方式；因此不适合作为经常需要查找和范围查找的数据库索引。</p>
<p><strong>二叉查找树：</strong></p>
<p>二叉树正常情况下可以进行折半查找而减少IO次数，但是二叉树不稳定，容易形成单链，查找时仍然很复杂，这种不稳定的结构选择设计时会尽量避免。</p>
<p><strong>平衡二叉树：</strong></p>
<p>平衡二叉树除了具备二叉查找树的特点，最主要的特征就是左右两个子树的层级最多相差1，在插入删除数据时要使用左旋/右旋操作来保持二叉树的平衡。平衡二叉树的查询性能最接近二分查找法，但是仍有一些不足之处：</p>
<ul>
<li>时间复杂度和树高有关，每个节点的读取对应一次磁盘IO操作，数据量大时查询性能会比较差</li>
<li>平衡二叉树不支持范围快速查找，范围查询时要从根节点进行多次遍历</li>
</ul>
<p><strong>B树：</strong></p>
<blockquote>
<p>MySQL的数据是存储在磁盘文件中的，查询处理数据时，需要把磁盘中的数据加载到内存中，磁盘IO操作非常耗时，因此我们优化的重点就是要尽量减少磁盘IO操作的次数。二叉树访问时每个节点就会发生一次磁盘IO，因此可以通过降低树的高度来减少磁盘IO的次数。</p>
<p>二叉树一个节点的数据结构为一个val+两个children指针，大概16(8+4*2)字节，而InnoDB一次IO默认会读取一页的数据量(16K)，这样每次IO操作数据空间利用率极低，为了最大化利用一次IO空间，就是在每个节点中存放多个元素，即每个节点存储更多的数据，这样就能将树的高度降的很低，这种结构就称为B树。</p>
</blockquote>
<ul>
<li><strong>B树是一种多叉平衡查找树，特点有：</strong><ul>
<li>B树的节点中存储着多个元素，每个内节点有多个分叉</li>
<li>节点中的元素包含键值和数据(data)，按照键值从小到大排列，即所有的节点中都存储了数据</li>
<li>父节点中的元素不会出现在子节点中</li>
<li>所有的叶子节点都位于同一层，即叶子有着相同的深度，叶子节点之间没有指针连接</li>
</ul>
</li>
<li>B树结构图</li>
<li>使用B树进行查找的过程：<ul>
<li>B树每个节点中的数据是一次IO操作读取出来的</li>
<li>B树种每个节点种存放许多个键值，IO操作读取到内存种后，会进行遍历比较，再查找指定的节点或根据键值返回数据</li>
<li>多叉树、每个节点存放多个键值，大大减少了树的高度，减少了磁盘IO的次数</li>
</ul>
</li>
<li>B树的不足点：<ul>
<li>B树不支持范围查询的快速查找，如果要进行范围查询，仍然是要从根节点多次遍历查询</li>
<li>因为B树节点中存放多个键以及对应的data，data对应数据库表的一行数据，随着表中的列增加，data占用空间变大，则一个节点中可以存放的数据个数就会减少，树会变高，查询时磁盘IO次数也会增加。</li>
</ul>
</li>
</ul>
<p><strong>B+树：</strong> </p>
<blockquote>
<p>B+树是在B树的基础上进行了改造，B+树相对B树的改变主要有：</p>
<ul>
<li>B+树<strong>只有叶子节点才会存储数据</strong>，非叶子节点只存放键(进行比较，可以覆盖索引)</li>
<li>B+树<strong>叶子节点之间使用双向指针连接</strong>，最底层叶子节点形成了双向有序链表</li>
<li>B+树由于只在叶子节点中存放数据，因此<strong>叶子节点中包含了所有的索引项</strong>(查找并返回数据)</li>
</ul>
<p>由于数据都放在了叶子节点中，非叶子节点只存放键值，<strong>每个节点中可以存放更多的索引键值</strong>，因此从理论上讲B+树高度是要比B树低的。</p>
<p>虽然非叶子节点中不存放数据，但是也存在<strong>覆盖索引</strong>的情况，如果索引中数据(key/id)满足要求，也可能直接返回，无需检索到最底层的叶子节点。</p>
</blockquote>
<ul>
<li><p><strong>等值查询：</strong></p>
<blockquote>
<p>B+树进行等值查询时，在叶子节点找到节点时，取出对应的data，如果data是实际的数据(<strong>InnoDB中叶子节点存放数据</strong>)则直接返回；如果是对应的地址(<strong>MyIsam中存放的是磁盘地址</strong>)，则根据磁盘地址到磁盘中取出数据，完成查询。</p>
</blockquote>
</li>
<li><p><strong>范围查询：</strong></p>
<blockquote>
<p>B+树进行范围查询时，首先进行范围左端点的查询，与等值查询一样完成当前值的查询，之后会直接使用叶子节点的链表进行后续的查询，读取其他磁盘页，最终完成整个范围查询（叶子节点中整体有序）。</p>
</blockquote>
</li>
<li><p><strong>总结：</strong>B+树保证了等值查询是快速的，也保证了范围查询的快速查找。</p>
</li>
</ul>
<h3 id="4-4-索引的使用"><a href="#4-4-索引的使用" class="headerlink" title="4.4 索引的使用"></a>4.4 索引的使用</h3><blockquote>
<p>具体索引使用过程，以MyIsam存储引擎中的索引和InnoDB存储引擎中索引的使用不同来介绍</p>
</blockquote>
<p><strong>MyIsam存储引擎：</strong> </p>
<blockquote>
<p>MyIsam引擎中也是使用的B+树构建索引树，叶子节点存放的<strong>键是列的值</strong>，data是<strong>索引所在行的磁盘地址</strong>。</p>
</blockquote>
<p>MyIsam中的主键索引和辅助索引的结构是完全一样的，叶子节点存储的数据都是记录行所在的磁盘地址，查询数据时，最终要根据磁盘地址读取数据。</p>
<p>**InnoDB存储引擎： ** </p>
<blockquote>
<p>InnoDB存储引擎相比于MyIsam可以定义自增主键，且主键索引更为特殊，也叫做聚簇索引，所有非主键索引都叫做辅助索引。</p>
<p><strong>规则：</strong> InnoDB会将定义的主键列作聚簇索引；如果没有定义主键则会选择第一个不为null的唯一索引作聚簇索引；如果都没有，则会生成一个隐式字段构建聚簇索引。</p>
</blockquote>
<ul>
<li><strong>聚簇索引：</strong> InnoDB的索引中，只有聚簇索引的叶子节点存放data数据，使用主键查询到叶子节点后返回数据</li>
<li><strong>辅助索引：</strong> 辅助索引的叶子节点存放的都是<strong>主键值</strong>，使用辅助索引时会先查询到叶子节点中的主键，根据主键值去到聚簇索引中进行<strong>回表查询</strong>data</li>
<li><strong>联合索引：</strong> 联合索引创建时，是以联合索引中列的数据进行排序，因此使用联合索引进行查询是，要符合最左匹配原则，即检索列的顺序要和创建索引时顺序一致，否则不会走索引。</li>
<li><strong>覆盖索引：</strong> 覆盖索引是一种常见的优化手段，即在使用辅助索引时，要先拿到主键索引再回表查询数据，如果当前所有中包含了需要查询的所有内容（字段值+主键值），则不需要进行主键的回表查询，即覆盖索引。</li>
</ul>
<p><strong>数据库索引优化手段：</strong></p>
<ol>
<li><p>避免回表</p>
<img src="assets/image-20201108193036198.png" alt="image-20201108193036198" style="zoom:80%;" />
</li>
<li><p>使用联合索引</p>
<ul>
<li>使用联合索引替代多个单列索引，不仅可以<strong>节省空间</strong>，还<strong>更容易使用到覆盖索引</strong></li>
<li>联合索引创建时，将频繁使用、区分度高的列放在前边</li>
<li>如果当前索引存在频繁查询的返回列，可考虑将此列加入联合索引，更容易使用覆盖索引减少回表查询</li>
</ul>
</li>
</ol>
<blockquote>
<p><strong>为什么推荐使用整型自增主键而不是选择UUID？</strong> </p>
</blockquote>
<ul>
<li>UUID是字符串，比整型消耗更多的存储空间</li>
<li>B+树查找时要进行节点值的比较，整型值要比字符串更容易比较（字符串会依次比较每一个字符）</li>
<li>自增的整型索引在磁盘中存储时会连续存储，读取时也是连续；而UUID随机产生，读取时表中上下两条数据分散的，范围查询时效果差一些</li>
<li>插入或删除数据时，整型自增主键建树时不会破坏树结构，UUID新增数据时可能会引起树结构的重新构建</li>
</ul>
<h3 id="4-5-索引的使用语句"><a href="#4-5-索引的使用语句" class="headerlink" title="4.5 索引的使用语句"></a>4.5 索引的使用语句</h3><figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="comment">---创建索引		如果是BLOB、TEXT类型，必须指定length </span></span><br><span class="line"><span class="keyword">CREATE</span> [<span class="keyword">UNIQUE</span>] <span class="keyword">INDEX</span> [indexname] <span class="keyword">ON</span> [tablename](columnName(<span class="keyword">length</span>));</span><br><span class="line"><span class="comment">---修改表结构(添加索引) </span></span><br><span class="line">	<span class="comment">---- PRIMARY Key / UNIQUE [indexname] / INDEX [indexname] / FULLTEXT [indexname]</span></span><br><span class="line">	<span class="comment">---- 主键索引		唯一索引				普通索引			全文索引</span></span><br><span class="line"><span class="keyword">ALTER</span> <span class="keyword">table</span> [tablename] <span class="keyword">ADD</span> [<span class="keyword">UNIQUE</span>] <span class="keyword">INDEX</span> [indexname](columnname)</span><br><span class="line"></span><br><span class="line"><span class="comment">---删除索引</span></span><br><span class="line"><span class="keyword">DROP</span> <span class="keyword">INDEX</span> [indexname] <span class="keyword">ON</span> [tablename]</span><br><span class="line"></span><br><span class="line"><span class="comment">---查看索引信息	\G用来格式化输出信息</span></span><br><span class="line"><span class="keyword">SHOW</span> <span class="keyword">INDEX</span> <span class="keyword">FROM</span> [tablename] \G</span><br></pre></td></tr></table></figure>

<h2 id="5-MySQL事务"><a href="#5-MySQL事务" class="headerlink" title="5. MySQL事务"></a>5. MySQL事务</h2><blockquote>
<p>参考文章： <a href="https://mp.weixin.qq.com/s?__biz=MzUyNjQxNjYyMg==&mid=2247489683&idx=2&sn=0bfbe8def3331404ef13a0da7b10334e&chksm=fa0e7712cd79fe04c2bc72866ee72632984391ceab0f5379c3ea37819434434c8033fab26418&mpshare=1&scene=24&srcid=&sharer_sharetime=1591506346818&sharer_shareid=c90d89447bde2426811e6f3556bd276d&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=Ae/+QZtcKepQB9ivq5tYokc=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">彻底搞懂MySQL事务日志</a>  <a href="https://mp.weixin.qq.com/s?__biz=MzI4Njg5MDA5NA==&mid=2247486783&idx=1&sn=5d794bbb97b09a9b097200ac4790d058&chksm=ebd74c3edca0c5288742261c856c907022e4524d70ef535986da3492ce4af8e84936a2555d8c&scene=0&xtrack=1&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=AcBeXoujtIA7DwjZRNfjukg=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">MySQL事务日志-原来MySQL面试还会问这些</a>  </p>
<p><a href="https://mp.weixin.qq.com/s?__biz=Mzg2MjEwMjI1Mg==&mid=2247501550&idx=3&sn=1a212bfc737981df2c942860e8e7dbe1&chksm=ce0e716df979f87b83ca1719f330c4fa1b4b9f1823ceb1483ae0539fc2105c3029efc06133e3&mpshare=1&scene=24&srcid=0916Evt5W1Nj6vj2ViKufZt1&sharer_sharetime=1600214786578&sharer_shareid=c90d89447bde2426811e6f3556bd276d&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=AbuaVTwtvxiuIecRXmQVX00=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">必须了解的 MySQL 三大日志</a>  <a href="https://mp.weixin.qq.com/s?__biz=MzAwNDA2OTM1Ng==&mid=2453147047&idx=1&sn=2ba0aa166bb7686ec90af4cded01f254&chksm=8cfd2f24bb8aa63207d72df02c01b1ad1dce63a39302ec1b73b868c44ea807c78081f6d5a9ee&mpshare=1&scene=24&srcid=1013o3NNMQV20q0AWX2pBnEF&sharer_sharetime=1602557171611&sharer_shareid=c90d89447bde2426811e6f3556bd276d&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=ASqhmE3uXyU2dBJ5UDHImwk=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">MySQL事务与MVCC机制</a>  <a href="https://mp.weixin.qq.com/s?__biz=Mzg2OTA0Njk0OA==&mid=2247486625&idx=2&sn=e235dab2757739438b8f33d205a9327f&chksm=cea2436af9d5ca7c9a1a8db9d020f71205687beca23ac958f9c9a711ee0185cab30173ad2b1a&scene=0&xtrack=1&key=&ascene=14&uin=MTU0NTA1NjAwMQ==&devicetype=Windows+10+x64&version=62090523&lang=zh_CN&exportkey=ASpvLKcaNTSftOGcA7F/YJs=&pass_ticket=WRKQP3G1bylvRFp+5dyKejmrjQcdw0gXiZjVA4XQVd8YXHZJBv66H4lgfERtCxdO&wx_header=0">MySQL事务的10个问题</a> </p>
</blockquote>
<h3 id="5-1-事务特性（ACID）"><a href="#5-1-事务特性（ACID）" class="headerlink" title="5.1 事务特性（ACID）"></a>5.1 事务特性（ACID）</h3><ul>
<li>原子性（Atomicity）：语句要么全执行，要么全不执行，是事务最核心的特性，事务本身就是以原子性来定义的，主要是基于<strong>undo log</strong>日志来实现的。</li>
<li>一致性（Consistency）：事务追求的最终目标，一致性的实现需要数据库层面的保障，也需要应用层面的保障。数据库的完整性约束没有被破坏。</li>
<li>隔离性（Isolation）：保证事务执行尽可能不受其他事务影响，InnoDB默认的隔离级别是RR，RR的实现主要基于<strong>锁机制（行锁）</strong>、<strong>数据的隐藏列（MVCC）</strong>、<strong>undo log</strong>、和类<strong>next-key lock机制</strong>。</li>
<li>持久性（Durability）：保证事务提交后不会因为宕机等原因导致数据丢失，主要基于<strong>redo log</strong> 实现。</li>
</ul>
<h3 id="5-2-并发读写问题"><a href="#5-2-并发读写问题" class="headerlink" title="5.2 并发读写问题"></a>5.2 并发读写问题</h3><blockquote>
<p>在并发情况下，MySQL的同时读写可能会导致三类问题：脏读、不可重复读、幻读</p>
</blockquote>
<ul>
<li><strong>脏读：</strong>当前事务中读取到其他事务未提交的数据，就是脏读<ul>
<li>事务A读取数据时，读取到事务B未提交的数据，而事务B最后没有顺利提交，导致事务回滚，这时事务A读到的内容就是脏数据。</li>
</ul>
</li>
<li><strong>不可重复读：</strong>在事务A中现后两次读取同一个数据，但两次读取的结果不一样；<ul>
<li>脏读是读到了其他事务未提交的数据，而不可重复读是读取到了其他事务新提交的内容，即事务A在执行的过程中，数据被其他事务修改了</li>
</ul>
</li>
<li><strong>幻读：</strong>事务A中按照某个条件现后两次查询数据库，两次结果的行数不同，这种现象称为幻读。<ul>
<li>不可重复读是两次读取的数据发生了变化，重在更新；幻读则是两次读取的数据行数不同，重在有新数据插入。</li>
</ul>
</li>
</ul>
<h3 id="5-2-事务隔离级别"><a href="#5-2-事务隔离级别" class="headerlink" title="5.2 事务隔离级别"></a>5.2 事务隔离级别</h3><blockquote>
<p>参考文章：<a href="https://tech.meituan.com/2014/08/20/innodb-lock.html">Innodb中的事务隔离级别和锁的关系</a>  </p>
</blockquote>
<p><strong>事务的隔离级别</strong></p>
<ul>
<li>Read UnCommitted（读未提交）：可能发生脏读、不可重复读、幻读</li>
<li>Read Committed（读已提交）：不会发生脏读，可能发生不可重复读、幻读</li>
<li>Repeatable Read（可重复读）：不会发生脏读、不可重复读，可能发生幻读</li>
<li>Serializable（可串行化）：不会发生脏读、不可重复读、幻读</li>
</ul>
<h3 id="MVCC"><a href="#MVCC" class="headerlink" title="MVCC"></a><strong>MVCC</strong></h3><blockquote>
<p>MVCC（Muti-Version Concurrency Control，多版本并发控制协议），就是用来实现可重复读隔离级别的。</p>
<p>MVCC的特点就是，<strong>在同一时刻，不同事务可以读取到不同版本的数据</strong>，从而解决脏读和不可重复读的问题。</p>
</blockquote>
<ul>
<li>MVCC实际上就是通过数据的<strong>隐藏列</strong>和<strong>回滚日志（undo log）</strong>，实现多个版本数据的共存，这样的好处就是，使用MVCC读取数据时，不用加锁就可以避免同时读写的冲突。</li>
<li>MVCC的每一行数据重会保存隐藏列，如<strong>创建时版本号</strong>、<strong>删除时版本号</strong>、<strong>指向undo log的回滚指针</strong>。每次开始新的事务，版本号就会递增，MVCC机制下，事务会查询<strong>小于等于创建版本号</strong>和<strong>大于等于删除版本号</strong>的数据，来保证数据的可重复读。</li>
<li><strong>next-key lock：</strong> 行锁的一种，即Record Lock（行锁）+gap lock（间隙锁），RR级别下就是<strong>通过next-key lock来避免幻读现象</strong>，其会将查询数据的数据行以及数据行的间隙进行加锁。防止其他事务在这个间隙修改或插入数据。<ul>
<li>间隙锁必须保证查询条件有索引，否则会加表锁而不是行锁（无索引时扫描全表）</li>
</ul>
</li>
<li>MVCC是快照读来避免幻读，间隙锁是当前读中避免幻读</li>
</ul>
<h3 id="5-3-事务日志"><a href="#5-3-事务日志" class="headerlink" title="5.3 事务日志"></a>5.3 事务日志</h3><blockquote>
<p>MySQL日志系统是数据库的重要组件，用于记录数据库的更新和修改，在数据库发生故障时可以通过不同的日志记录恢复数据，日志系统决定着MySQL的鲁棒性和稳健性。</p>
<p>MySQL的日志有很多种，如二进制日志（bin log）、错误日志、查询日志、慢查询日志等，此外，InnoDB存储引擎还提供了两种日志，redo log（重做日志）和undo log（回滚日志）</p>
</blockquote>
<ul>
<li><p><strong>重做日志（redo log）：</strong> 重做日志是InnoDB引擎层的日志，用来记录事务操作引起数据的变化，记录的是<strong>数据页的物理修改</strong>。（即什么地方从什么改成了什么这样）</p>
<ul>
<li><strong>预写式技术（Write Ahead logging）</strong>：InnoDB引擎对数据的更新，是<strong>先将更新记录写入redo log日志</strong>，然后<strong>会在系统空闲的时候或是按照设定的更新策略将日志种的内容更新到磁盘中</strong>，这就是所谓的预写式技术，这种技术可以大大减少IO操作的频率，提升数据刷新的效率。</li>
<li><strong>脏数据刷盘：</strong>redo log日志的大小是固定的，为了能够持续不断的对更新记录进行写入，redo log中设置了两个标识位置，<strong>checkpoint（记录擦除的位置）</strong>和<strong>write_pos（记录写入的位置）</strong>。移动是循环的，类似圆周运动，当写位置追上擦除位置时，会停下来进行脏数据刷盘（即将redo log中的数据写入到磁盘中）。</li>
<li><strong>缓冲池（buffer pool）：</strong> 在内存中分配的一个区域，包含了磁盘中部分数据页的映射，作为访问数据库的缓冲。<ul>
<li>当请求读取数据时，会判断是否在缓冲池命中，如果未命中才会在磁盘上进行检索后放入缓冲池；</li>
<li>当请求写入数据时，会先写入缓冲池，缓冲池中修改的数据会定期刷新到磁盘中，即刷脏</li>
<li>当数据修改时，除了会修改缓冲池中数据，还会在redo log中记录当前操作，事务提交时，会根据redo log的记录的数据进行刷盘。</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>二进制日志（bin log）</strong>：二进制日志（bin log）是服务层的日志，被称为归档日志。bin log 主要是记录数据库的变化情况，内容包括数据库所有的更新操作。所有涉及数据变动的操作，都要记录进二进制日志中。<strong>有了binlog可以很方便的对数据进行复制和备份，因而常用作主从数据的同步</strong>。</p>
<ul>
<li>redo log记录的是物理日志，即对某个数据进行了怎么样的修改；而<strong>binlog是逻辑日志</strong>，记录的是SQL语句的原始逻辑。</li>
<li>redo log是InnoDB引擎特有的，作用在引擎层；binlog是通过MySQL的服务层实现的，所有引擎都可以使用</li>
<li>redo log日志文件的空间固定，因此使用时是循环写入和擦除；binlog日志文件的空间不固定，写完会切换下一个文件，因此是追加写入，不会覆盖已有文件。</li>
</ul>
</li>
<li><p>一条SQL语句执行时，涉及到binlog和redo log 的读写：</p>
<img src="assets/image-20201125214003798.png" alt="image-20201125214003798" style="zoom:80%;" /> 

<ul>
<li>MySQL执行更新语句时，服务层进行语句的解析和执行，引擎层进行数据的提取和存储；同时在服务存对binlog写入，在引擎层进行redo log的写入。</li>
<li>对redo log写入时有两个阶段的提交，一个是binlog写入之前prepare状态的写入，二是binlog写入之后commit状态的写入，保证了binlog和redo log日志的一致性。</li>
</ul>
</li>
<li><p><strong>回滚日志（undo log）：</strong> 回滚日志也是InnoDB引擎提供的日志，是对数据进行回滚。在事务对数据库进行修改时，<strong>InnoDB引擎不仅会记录redo log，还会生成对应的undo log日志</strong>，如果事务失败执行rollback，事务回滚，就会<strong>利用undo log中的信息将数据回滚到之前的样子</strong>。</p>
<ul>
<li>与redo log不同的是，undo log也是逻辑日志，<strong>记录了SQL语句执行相关的信息</strong>，当发生回滚时，<strong>InnoDB引擎会根据undo log日志中的记录做与之前相反的工作。</strong></li>
<li>undo log日志有两个作用，一个是提供<strong>事务回滚</strong>，一个是实现<strong>MVCC</strong>。</li>
</ul>
</li>
</ul>
<h2 id="6-MySQL锁机制"><a href="#6-MySQL锁机制" class="headerlink" title="6. MySQL锁机制"></a>6. MySQL锁机制</h2><h2 id="7-MySQL主从复制"><a href="#7-MySQL主从复制" class="headerlink" title="7. MySQL主从复制"></a>7. MySQL主从复制</h2><blockquote>
<p>主从复制就是从原来的数据库复制一个完全一样的数据库，并保持主、从数据库数据的一致性。</p>
<p>主从复制的原理实际上就是通过binlog日志实现的，binlog日志中保存了数据库中所有SQL语句，通过对binlog日志中SQL的复制，然后再进行语句的执行即可实现从数据库与主数据库的同步。</p>
</blockquote>
<img src="assets/image-20201125215032778.png" alt="image-20201125215032778" style="zoom:80%;" /> 

<p>主从复制的过程如图，主要涉及到三个线程</p>
<ul>
<li>主服务器中的发送线程：负责将binlog日志发送到从服务器</li>
<li>从服务器上的I/O线程：用来读取主服务器发送过滤的binlog日志内容，并拷贝到本地中继日志中</li>
<li>从服务器的SQL线程：用来读取中继日志中关于数据更新的SQL语句并执行，实现主从数据一致</li>
<li>主从复制的好处有：<ul>
<li>通过复制实现数据的异地备份，避免数据丢失</li>
<li>实现架构的扩展，业务量越来越大时，I/O访问量过高时，采用多库存储，降低磁盘I/O频率</li>
<li>实现读写分离，使数据库能支持更大的并发</li>
<li>实现服务器的负载均衡，通过在主服务器和从服务器之间切分处理客户查询的负荷</li>
</ul>
</li>
</ul>
<h2 id="8-MySQL存储过程"><a href="#8-MySQL存储过程" class="headerlink" title="8. MySQL存储过程"></a>8. MySQL存储过程</h2><h2 id="9-MySQL优化"><a href="#9-MySQL优化" class="headerlink" title="9. MySQL优化"></a>9. MySQL优化</h2>]]></content>
      <tags>
        <tag>MySQL</tag>
        <tag>面试</tag>
      </tags>
  </entry>
  <entry>
    <title>经典排序算法</title>
    <url>/classical-sorting-algorithm/</url>
    <content><![CDATA[<p>快速排序、堆排序、归并排序</p>
<a id="more"></a>

<h1 id="排序算法"><a href="#排序算法" class="headerlink" title="排序算法"></a>排序算法</h1><h2 id="排序数组"><a href="#排序数组" class="headerlink" title="排序数组"></a>排序数组</h2><blockquote>
<p>给你一个整数数组<code>nums</code>，请你将该数组升序排列。</p>
<p><a href="https://leetcode-cn.com/problems/sort-an-array/">力扣912：排序数组</a> </p>
<p>示例1：</p>
<img src="/imgs/classical-sorting-algorithm/image-20201128201336489.png" alt="image-20201128201336489" style="zoom:100%;" />

<p>示例2：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201128201355458.png" alt="image-20201128201355458"></p>
<p>Tips：常见的排序算法都可以，但是尽量使用性能更好的快排、堆排序、归并排序。</p>
</blockquote>
<h3 id="快速排序（重要）"><a href="#快速排序（重要）" class="headerlink" title="快速排序（重要）"></a>快速排序（重要）</h3><blockquote>
<p>思想：递归思想，</p>
<ul>
<li>递归结束条件：左端点大于等于右端点，说明只有一个元素，结束</li>
<li>对0至n-1的元素进行排序，选择第一个数作为基准值pivot</li>
<li>使用左右双指针，只要满足左边位置小于右边，就进行循环<ul>
<li>右指针从右端点向左检查，发现小于基准值的就覆盖到左边，右指针停止占位</li>
<li>左指针从左端点向右检查，发现大于基准值的覆盖掉右指针处值，左指针停止占位</li>
<li>循环结束后左右指针重合，将基准值放入此处，完成一轮循环</li>
</ul>
</li>
<li>以上一轮基准元素确定的位置为中心，左边排序，右边排序</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] sortArray(<span class="keyword">int</span>[] nums) &#123;</span><br><span class="line">    <span class="comment">//手写快排，类库中sort方法也是封装的快排</span></span><br><span class="line">    <span class="comment">//以第一个为基准，右侧元素进行排序，找到当前元素的位置</span></span><br><span class="line">    <span class="comment">//递归至只有一个元素</span></span><br><span class="line">    <span class="comment">//执行用时：5 ms, 在所有 Java 提交中击败了92.45%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：46 MB, 在所有 Java 提交中击败了60.89%的用户</span></span><br><span class="line">    quickSort(nums,<span class="number">0</span>,nums.length-<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> nums;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">quickSort</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> l, <span class="keyword">int</span> r)</span></span>&#123;</span><br><span class="line">    <span class="comment">//只剩一个元素需要排序时，递归终止</span></span><br><span class="line">    <span class="keyword">if</span>(l &gt;= r) <span class="keyword">return</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//使用左端点值作为基准值</span></span><br><span class="line">    <span class="keyword">int</span> pivot = nums[l], start = l, end = r;</span><br><span class="line">    <span class="keyword">while</span>(start &lt; end)&#123;</span><br><span class="line">        <span class="comment">//从右边找到小于基准的数值</span></span><br><span class="line">        <span class="keyword">while</span>(start &lt; end &amp;&amp; nums[end] &gt;= pivot) end--;</span><br><span class="line">        nums[start] = nums[end];</span><br><span class="line">        <span class="comment">//从左边找到大于基准的数值</span></span><br><span class="line">        <span class="keyword">while</span>(start &lt; end &amp;&amp; nums[start] &lt;= pivot) start++;</span><br><span class="line">        nums[end] = nums[start];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//基准值放入确定位置</span></span><br><span class="line">    nums[start] = pivot;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//左边快排</span></span><br><span class="line">    quickSort(nums,l,start-<span class="number">1</span>);</span><br><span class="line">    <span class="comment">//右边快排</span></span><br><span class="line">    quickSort(nums,start+<span class="number">1</span>,r);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="堆排序（重要）"><a href="#堆排序（重要）" class="headerlink" title="堆排序（重要）"></a>堆排序（重要）</h3><blockquote>
<p>思想：堆是存储在数组中的，但是结构是树的思想。</p>
<ul>
<li><p>大（小）根堆则有：根节点比左右子节点都大（小），子节点也满足。</p>
</li>
<li><p>根堆在数组中，i位置点的左子节点是<code>2*i+1</code>，右子结点是<code>2*i+2</code> </p>
</li>
</ul>
<p>使用堆进行排序时：可建大根堆或小根堆，具体步骤：</p>
<ul>
<li><strong>对整个数组建立根堆</strong>，<ul>
<li>根据堆的二叉树思想从n/2位置处，向前遍历，因后边一半都是叶子结点</li>
<li>判断：如果当前节点左子节点i*2+1小于堆长度，说明存在；右子节点同理</li>
<li>遍历过程中判断当前结点和其左右子节点值大小，将最大的放在根上（即当前位置）</li>
<li>如果与子节点进行了交换，则要跟踪交换后的位置，为根时是否满足大根堆的条件（连续下沉）</li>
</ul>
</li>
<li>建堆完成后，将根（最大值）与最后一个结点进行交换（最大值就应该放在最后）<ul>
<li>数组长度-1，然后进行根堆的重建（结点的下沉）</li>
<li>以以一个结点为根，长度为<code>len-1</code>，进行大根堆的建立（调整堆）（与建堆时方法一致，复用）</li>
<li>最后循环至数组长度为<code>len=1</code>，完成排序</li>
</ul>
</li>
<li><strong>主要核心内容：</strong><ol>
<li>建堆，即从第一个非叶子节点开始进行调整堆（连续下沉）</li>
<li>将最大值（根）换到最后一位，堆长度-1</li>
<li>对整个堆进行调整（连续下沉）</li>
<li>循环2、3步骤</li>
</ol>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] sortArray(<span class="keyword">int</span>[] nums) &#123;</span><br><span class="line">    <span class="comment">//使用堆排序，建堆，动态调整堆（下沉法）</span></span><br><span class="line">    <span class="comment">//执行用时：10 ms, 在所有 Java 提交中击败了30.65%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：45.7 MB, 在所有 Java 提交中击败了80.61%的用户</span></span><br><span class="line">    <span class="comment">//首先，建立一个大根堆</span></span><br><span class="line">    buildMaxHeap(nums);</span><br><span class="line">    <span class="comment">//然后，将大根堆的根（最大值）与最后一个值交换，堆长度减一</span></span><br><span class="line">    <span class="keyword">int</span> len = nums.length-<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(len &gt; <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="keyword">int</span> temp = nums[<span class="number">0</span>];</span><br><span class="line">        nums[<span class="number">0</span>] = nums[len];</span><br><span class="line">        nums[len] = temp;</span><br><span class="line">        len--;</span><br><span class="line">        adjectHeap(nums,<span class="number">0</span>,len);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//堆排序</span></span><br><span class="line"><span class="comment">//建立大根堆</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">buildMaxHeap</span><span class="params">(<span class="keyword">int</span>[] nums)</span></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = nums.length/<span class="number">2</span>; i &gt;= <span class="number">0</span>; i--)&#123;</span><br><span class="line">        adjectHeap(nums,i,nums.length-<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//调整堆（根-最大值与最后一个值交换），使用下沉法调整n-1个结点</span></span><br><span class="line"><span class="comment">//指定形成堆/调整堆的区间，即从i-len范围内</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">adjectHeap</span><span class="params">(<span class="keyword">int</span>[] nums,<span class="keyword">int</span> i, <span class="keyword">int</span> len)</span></span>&#123;</span><br><span class="line">    <span class="comment">//非叶子节点时去判断是否要下沉,即当前i的左子节点是否在i-len范围内，</span></span><br><span class="line">    <span class="comment">//不在说明i不是非叶子节点（当前范围堆中的非叶子节点）</span></span><br><span class="line">    <span class="keyword">while</span>(i*<span class="number">2</span>+<span class="number">1</span> &lt;= len)&#123;</span><br><span class="line">        <span class="comment">//从第一个非叶子节点（当前i结点）开始，</span></span><br><span class="line">        <span class="comment">//将子节点中大于当前值 且 最大的值换上来，</span></span><br><span class="line">        <span class="comment">//然后判断换下去的值在其位置上是否是满足大根堆（连续下沉方法）</span></span><br><span class="line">        <span class="comment">//如果当前结点时最大值，没有下沉，则不需要继续判断</span></span><br><span class="line">        <span class="comment">//如果下沉到叶子结点了，也不需要再判断</span></span><br><span class="line">        <span class="keyword">int</span> max = i;</span><br><span class="line">        <span class="comment">//判断左子节点是否更大</span></span><br><span class="line">        <span class="keyword">if</span>(i*<span class="number">2</span>+<span class="number">1</span> &lt;= len &amp;&amp; nums[i*<span class="number">2</span>+<span class="number">1</span>] &gt; nums[max]) max = i*<span class="number">2</span>+<span class="number">1</span>;</span><br><span class="line">        <span class="comment">//判断右子节点是否更大   </span></span><br><span class="line">        <span class="keyword">if</span>(i*<span class="number">2</span>+<span class="number">2</span> &lt;= len &amp;&amp; nums[i*<span class="number">2</span>+<span class="number">2</span>] &gt; nums[max]) max = i*<span class="number">2</span>+<span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//如果需要下沉</span></span><br><span class="line">        <span class="keyword">if</span>(max != i)&#123;</span><br><span class="line">            <span class="keyword">int</span> temp = nums[i];</span><br><span class="line">            nums[i] = nums[max];</span><br><span class="line">            nums[max] = temp;</span><br><span class="line"></span><br><span class="line">            <span class="comment">//当前i要跟踪下沉到的新位置，判断是否需要继续下沉</span></span><br><span class="line">            i = max; </span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">break</span>;    <span class="comment">//如果不需要下沉，说明以当前i为根的，已经是大根堆了</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="归并排序（重要）"><a href="#归并排序（重要）" class="headerlink" title="归并排序（重要）"></a>归并排序（重要）</h3><blockquote>
<p>归并排序是利用了分治的塑像来对序列进行排序，将一个长为n的待排序的序列，我们将其分解成两个长度为n/2的子序列。每次递归调用函数使两个子序列有序，然后我们再线性合并两个有序的子序列使整个序列有序。</p>
<p>具体实现步骤：</p>
<ul>
<li>针对一个长度为n的序列进行排序时，</li>
<li>取序列中值，递归使用<code>margeSort</code>方法来对前半部分排序，递归使用<code>margeSort</code>方法对后半部分排序</li>
<li>对已经排好序列的[l-mid]和[mid+1,r]，使用双指针的思想合并两个有序数组<ul>
<li>两个指针起始位置比较大小，取值小的加入临时数组，指针前进，</li>
<li>至其中一个指针到达终点，将另外一部分值全部添加到后边</li>
<li>将临时数组中有序值复制到原数组中，最后整个数组就是有序的</li>
</ul>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] sortArray(<span class="keyword">int</span>[] nums) &#123;</span><br><span class="line">    <span class="comment">//使用归并排序</span></span><br><span class="line">    <span class="comment">//对前后两部分分别排序，有序后使用双指针思想合并两个有序数组</span></span><br><span class="line">    <span class="comment">//执行用时：8 ms, 在所有 Java 提交中击败了44.08%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：45.7 MB, 在所有 Java 提交中击败了85.15%的用户</span></span><br><span class="line">    margeSort(nums,<span class="number">0</span>,nums.length-<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> nums;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//归并排序</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">margeSort</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> l, <span class="keyword">int</span> r)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(l &gt;= r) <span class="keyword">return</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> mid = (l+r) / <span class="number">2</span>;</span><br><span class="line">    <span class="comment">//递归排序前半部分和后半部分</span></span><br><span class="line">    margeSort(nums,l,mid);</span><br><span class="line">    margeSort(nums,mid+<span class="number">1</span>,r);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//合并两个有序数组（双指针思想）</span></span><br><span class="line">    <span class="keyword">int</span> i = l, j = mid+<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span>[] temp = <span class="keyword">new</span> <span class="keyword">int</span>[r-i+<span class="number">1</span>];<span class="comment">//临时数组可以定义成最长全局变量，每次使用长度不同</span></span><br><span class="line">    <span class="keyword">int</span> index = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">//合并两个有序数组</span></span><br><span class="line">    <span class="keyword">while</span>(i &lt;= mid &amp;&amp; j &lt;= r)&#123;</span><br><span class="line">        <span class="keyword">if</span>(nums[i] &lt;= nums[j]) temp[index++] = nums[i++];</span><br><span class="line">        <span class="keyword">else</span> temp[index++] = nums[j++];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span>(i &lt;= mid) temp[index++] = nums[i++];</span><br><span class="line">    <span class="keyword">while</span>(j &lt;= r) temp[index++] = nums[j++];</span><br><span class="line">	</span><br><span class="line">    <span class="comment">//将排序好的序列从临时数组移动至源数组</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> count = <span class="number">0</span>; count &lt; temp.length; count++)&#123;</span><br><span class="line">        nums[l+count] = temp[count];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="快排的应用"><a href="#快排的应用" class="headerlink" title="快排的应用"></a>快排的应用</h2><h3 id="求解TopK问题（请看TopK问题专栏）"><a href="#求解TopK问题（请看TopK问题专栏）" class="headerlink" title="求解TopK问题（请看TopK问题专栏）"></a>求解<code>TopK</code>问题（请看<code>TopK</code>问题专栏）</h3><h3 id="把数组排成最小的数"><a href="#把数组排成最小的数" class="headerlink" title="把数组排成最小的数"></a>把数组排成最小的数</h3><blockquote>
<p>输入一个非负数整数数组，把数组里所有数字拼接起来排成一个数，打印能拼接出的所有数字中最小的一个。</p>
<p><a href="https://leetcode-cn.com/problems/ba-shu-zu-pai-cheng-zui-xiao-de-shu-lcof/">力扣-剑指Offer45：把数组排成最小的数</a> </p>
<p>示例1：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201203171718659.png" alt="image-20201203171718659"> </p>
<p>示例2：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201203171732421.png" alt="image-20201203171732421"> </p>
<p>Tips：可以使用快排的思想，把数组中数字作为字符串，进行字符串之间的比较，将数组中字符串排序，最终得到有序的字符串排列就是最小数</p>
<ul>
<li>在比较<code>x,y</code>大小时，使用 <code>x+y</code> 和 <code>y+x</code> 进行比较，如 <code>&quot;3&quot;+&quot;30&quot; = &quot;330&quot;</code> 大于 <code>&quot;30&quot;+&quot;3&quot; = &quot;303&quot;</code>  ,因此<code>&quot;3&quot;</code>大于<code>&quot;30&quot;</code></li>
<li>使用快排进行字符串排序，求得结果</li>
<li>使用了Java中<strong>字符串比较方法<code>str1.compareTo(str2); </code></strong><ul>
<li><code>结果=0:相等</code> </li>
<li><code>结果&gt;0:str1&gt;str2</code> </li>
<li><code>结果&lt;0:str1&lt;str2 </code> </li>
</ul>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">minNumber</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//把数组排成最小的数</span></span><br><span class="line">        <span class="comment">//如30，3排列要比3，30小，可以用字符串拼接303，330来比较大小</span></span><br><span class="line">        <span class="comment">//按照给定的顺序来将字符串数组进行排序，最后按照顺序输出即可</span></span><br><span class="line">        <span class="comment">//执行用时：5 ms, 在所有 Java 提交中击败了97.69%的用户</span></span><br><span class="line">        <span class="comment">//内存消耗：37.9 MB, 在所有 Java 提交中击败了89.79%的用户</span></span><br><span class="line">        <span class="comment">//将数字数组转成字符串数组</span></span><br><span class="line">        String[] strs = <span class="keyword">new</span> String[nums.length];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; nums.length; i++)&#123;</span><br><span class="line">            strs[i] = Integer.toString(nums[i]);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        quickSort(strs,<span class="number">0</span>,strs.length-<span class="number">1</span>);</span><br><span class="line">    </span><br><span class="line">    	<span class="comment">//也可以使用Java自带API，排序方法Arrays.sort()，传入排序的比较器</span></span><br><span class="line">        <span class="comment">//执行用时：6 ms, 在所有 Java 提交中击败了85.26%的用户</span></span><br><span class="line">        <span class="comment">//内存消耗：38 MB, 在所有 Java 提交中击败了86.94%的用户</span></span><br><span class="line">        <span class="comment">//可见API还是比自己手写要慢一些的，应该时封装的比较好</span></span><br><span class="line">        <span class="comment">//Arrays.sort(strs,(x,y) -&gt; (x+y).compareTo(y+x));    </span></span><br><span class="line">    </span><br><span class="line">        StringBuffer sb = <span class="keyword">new</span> StringBuffer();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; strs.length; i++)&#123;</span><br><span class="line">            sb.append(strs[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sb.toString();<span class="comment">//StringUtils.join(strs,&quot;&quot;);</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//快排思想排序</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">quickSort</span><span class="params">(String[] strs, <span class="keyword">int</span> start, <span class="keyword">int</span> end)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(start &gt;= end) <span class="keyword">return</span> ;</span><br><span class="line">        <span class="keyword">int</span> l = start, r = end;</span><br><span class="line">        String pivot = strs[l];</span><br><span class="line">        <span class="keyword">while</span>(l &lt; r)&#123;</span><br><span class="line">            <span class="comment">//如果右端有小于基准值的，则交换之</span></span><br><span class="line">            <span class="keyword">while</span>(l &lt; r &amp;&amp; (pivot + strs[r]).compareTo(strs[r] + pivot) &lt;= <span class="number">0</span>) r--;</span><br><span class="line">            strs[l] = strs[r];</span><br><span class="line">            <span class="comment">//如果左侧有大于基准值的，则交换之</span></span><br><span class="line">            <span class="keyword">while</span>(l &lt; r &amp;&amp; (pivot + strs[l]).compareTo(strs[l] + pivot) &gt;= <span class="number">0</span>) l++;</span><br><span class="line">            strs[r] = strs[l];</span><br><span class="line">        &#125;</span><br><span class="line">        strs[l] = pivot;</span><br><span class="line">		quickSort(strs,start,l-<span class="number">1</span>);</span><br><span class="line">        quickSort(strs,l+<span class="number">1</span>,end);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>







<h2 id="堆排序的应用"><a href="#堆排序的应用" class="headerlink" title="堆排序的应用"></a>堆排序的应用</h2><h3 id="求解TopK问题（请看TopK问题专栏）-1"><a href="#求解TopK问题（请看TopK问题专栏）-1" class="headerlink" title="求解TopK问题（请看TopK问题专栏）"></a>求解<code>TopK</code>问题（请看<code>TopK</code>问题专栏）</h3><h3 id="根据字符出现频率排序"><a href="#根据字符出现频率排序" class="headerlink" title="根据字符出现频率排序"></a>根据字符出现频率排序</h3><blockquote>
<p>给定一个字符串，请将字符串里的字符按照出现的频率降序排列。</p>
<p>示例1：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201204111729878.png" alt="image-20201204111729878"> </p>
<p>示例2：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201204111754216.png" alt="image-20201204111754216"> </p>
<p>示例3：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201204111809160.png" alt="image-20201204111809160"> </p>
<p>Tips：</p>
<ul>
<li>对于统计频率的问题，可以使用桶排序方法，先确定最大频率后根据最大频率创建桶，按照固定顺序输出。</li>
<li>使用堆排序（优先队列），根据字符的出现频率进行排序，建大根堆，最终依次输出堆的根节点</li>
</ul>
</blockquote>
<h2 id="归并排序的应用"><a href="#归并排序的应用" class="headerlink" title="归并排序的应用"></a>归并排序的应用</h2><h3 id="求数组中翻转对的数量"><a href="#求数组中翻转对的数量" class="headerlink" title="求数组中翻转对的数量"></a>求数组中翻转对的数量</h3><blockquote>
<p>题目：给定一个数组<code>nums</code>，如果i&lt;j且<code>nums[i] &gt; 2 * nums[j]</code> 我们就将 (i,j)称作一个重要<strong>翻转对</strong>。你需要返回给定数组中的重要翻转对的数量。</p>
<p><a href="https://leetcode-cn.com/problems/reverse-pairs/">力扣493：翻转对</a> （困难）</p>
<p><a href="https://leetcode-cn.com/problems/shu-zu-zhong-de-ni-xu-dui-lcof/">力扣-剑指Offer51：数组中的逆序对</a> （思想一样）</p>
<p>示例1：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201129205124584.png" alt="image-20201129205124584"> </p>
<p>示例2：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201129205134614.png" alt="image-20201129205134614"> </p>
<p>Tips：翻转对要求的是 i小于j的情况下，<code>nums[i] &gt; 2 * nums[j]</code> </p>
<ul>
<li><p>针对当前i，只需要考虑i之后的数字是否满足，而当其后元素有序时更容易计算</p>
</li>
<li><p>因此我们结合归并排序的思想，在每次分治排序时求出每个小数组中的<strong>翻转对</strong>数量</p>
</li>
<li><p>每次合并之前，要计算 前一个数组和后一个数组 中元素跨区间的翻转对数量</p>
<ul>
<li>如[1,2,3]和[2,3,5]合并时，合并之后的翻转对等于两个数组各自的翻转对数</li>
<li>再加上1,2,3分别在[2,3,5]中的翻转对数量，后边数组下标都大</li>
<li>就是[1,2,2,3,3,5]中总的翻转对数量</li>
</ul>
</li>
<li><p>总的算法步骤：</p>
<ul>
<li>针对整个数组进行归并排序</li>
<li>递归针对前一半和后一半进行归并排序</li>
<li>针对两个有序数组，累计计算跨区间的<strong>翻转对数量</strong> （每次合并完后，之前的跨区间就变成了当前数组对应的数量）</li>
<li>合并两个有序数组</li>
</ul>
<p>（这个思想想起来很困难呐~）</p>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">reversePairs</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//归并排序进行计算</span></span><br><span class="line">    <span class="comment">//执行用时：59 ms, 在所有 Java 提交中击败了63.72%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：48 MB, 在所有 Java 提交中击败了61.96%的用户</span></span><br><span class="line">    <span class="keyword">return</span> margeSort(nums,<span class="number">0</span>,nums.length-<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//使用归并排序进行计算</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">margeSort</span><span class="params">(<span class="keyword">int</span>[] nums,<span class="keyword">int</span> l, <span class="keyword">int</span> r)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(l &gt;= r) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> mid = (l+r) / <span class="number">2</span>;</span><br><span class="line">    <span class="comment">//递归使用归并排序</span></span><br><span class="line">    <span class="keyword">int</span> n1 = margeSort(nums,l,mid);</span><br><span class="line">    <span class="keyword">int</span> n2 = margeSort(nums,mid+<span class="number">1</span>,r);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//累计计算翻转对</span></span><br><span class="line">    <span class="keyword">int</span> cur = n1 + n2;</span><br><span class="line">    <span class="keyword">int</span>  m = mid+<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = l; i &lt;= mid; i++)&#123;</span><br><span class="line">        <span class="comment">//这样就没有这个精髓了，还是超时的</span></span><br><span class="line">        <span class="comment">// for(int j = mid+1; j &lt;= r; j++)&#123;</span></span><br><span class="line">        <span class="comment">//     if((long)nums[i] &gt; (long)nums[j] * 2)&#123;</span></span><br><span class="line">        <span class="comment">//         cur++;//j-(mid-1);</span></span><br><span class="line">        <span class="comment">//     &#125; </span></span><br><span class="line">        <span class="comment">// &#125;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//这样，才能充分使用这个有序的序列(精髓)</span></span><br><span class="line">        <span class="keyword">while</span>(m &lt;= r &amp;&amp; (<span class="keyword">long</span>)nums[i] &gt; (<span class="keyword">long</span>)nums[m] * <span class="number">2</span>) m++;</span><br><span class="line">        cur += m-(mid+<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//合并两个有序数组</span></span><br><span class="line">    <span class="keyword">int</span> i = l, j = mid+<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span>[] temp = <span class="keyword">new</span> <span class="keyword">int</span>[r-l+<span class="number">1</span>];</span><br><span class="line">    <span class="keyword">int</span> index = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span>(i &lt;= mid &amp;&amp; j &lt;= r)&#123;</span><br><span class="line">        temp[index++] = nums[i] &lt;= nums[j] ? nums[i++] : nums[j++];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span>(i &lt;= mid) temp[index++] = nums[i++];</span><br><span class="line">    <span class="keyword">while</span>(j &lt;= r) temp[index++] = nums[j++];</span><br><span class="line"></span><br><span class="line">    <span class="comment">//临时数组中的有序序列写入原数组中</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> k = <span class="number">0</span>; k &lt; temp.length; k++)&#123;</span><br><span class="line">        nums[l+k] = temp[k];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> cur;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





<h2 id="求解TopK的算法"><a href="#求解TopK的算法" class="headerlink" title="求解TopK的算法"></a>求解<code>TopK</code>的算法</h2><h3 id="数组中的第K个最大元素"><a href="#数组中的第K个最大元素" class="headerlink" title="数组中的第K个最大元素"></a>数组中的第K个最大元素</h3><blockquote>
<p>题目：在未排序的数组中找到第k个最大的元素。请注意，你需要找的是数组排序后的第k个最大的元素，而不是第k个不同的元素。</p>
<p><a href="https://leetcode-cn.com/problems/kth-largest-element-in-an-array/">力扣215：数组中的第K个最大元素</a> </p>
<p>示例1：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201129162443187.png" alt="image-20201129162443187"> </p>
<p>示例2：</p>
<p><img src="/imgs/classical-sorting-algorithm/image-20201129162456331.png" alt="image-20201129162456331"> </p>
<p>Tips：</p>
<ol>
<li>假设k总是有效的，k &gt;= 1 &amp;&amp; k&lt;=数组的长度</li>
<li>如果有相同的数，则数量累加，即 1122：最大和第二大都是2</li>
</ol>
<p>思路：对于<code>TopK</code>的问题，是排序问题的变形，重点在于排序的过程，经典<code>TopK</code>问题可以使用快速排序或堆排序来解决</p>
</blockquote>
<blockquote>
<p><strong>快速排序：</strong>快排的过程中，判断当前轮排序的数字位置与k的关系，</p>
<ul>
<li><p>正好是第k个数，则返回当前数值</p>
</li>
<li><p>位置小于第k个数（n-k），说明第k个在右边，则对右边继续使用快排</p>
</li>
<li><p>位置大于第k个数（n-k），说明第k个在左边，则对左边继续使用快排</p>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">findKthLargest</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//数组中的第K个最大元素</span></span><br><span class="line">        <span class="comment">//快排思想：</span></span><br><span class="line">            <span class="comment">//使用快排进行排序的过程中，每进行一轮排序就比较一下当前元素位置</span></span><br><span class="line">            <span class="comment">//如果当前元素位置是n-k，说明当前元素是第K大元素</span></span><br><span class="line">            <span class="comment">//如果小于 n-k，则对右边进行排序</span></span><br><span class="line">            <span class="comment">//如果大于 n-k，则对左边进行排序</span></span><br><span class="line">        <span class="comment">//执行用时：9 ms, 在所有 Java 提交中击败了36.21%的用户</span></span><br><span class="line">        <span class="comment">//内存消耗：38.6 MB, 在所有 Java 提交中击败了89.32%的用户</span></span><br><span class="line">        <span class="keyword">return</span> quickSortFind(nums,<span class="number">0</span>,nums.length-<span class="number">1</span>,k);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">quickSortFind</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> l, <span class="keyword">int</span> r, <span class="keyword">int</span> k)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> pivot = nums[l], left = l, right = r;</span><br><span class="line">        <span class="keyword">while</span>(left &lt; right)&#123;</span><br><span class="line">            <span class="keyword">while</span>(left &lt; right &amp;&amp; nums[right] &gt;= pivot) right--;</span><br><span class="line">            nums[left] = nums[right];</span><br><span class="line">            <span class="keyword">while</span>(left &lt; right &amp;&amp; nums[left] &lt;= pivot) left++;</span><br><span class="line">            nums[right] = nums[left];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(left &gt;=<span class="number">0</span> &amp;&amp; left &lt; nums.length) nums[left] = pivot;</span><br><span class="line">        <span class="keyword">if</span>(left == nums.length-k) <span class="keyword">return</span> nums[left];</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(left &lt; nums.length-k) <span class="keyword">return</span> quickSortFind(nums,left+<span class="number">1</span>,r,k);</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">return</span> quickSortFind(nums,l,left-<span class="number">1</span>,k);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



<blockquote>
<p><strong>堆排序：</strong>因为是求数组中的<code>TopK</code>问题，堆排序更合适，可使用大根堆或者小根堆，以求第K大为例</p>
<ul>
<li>大根堆：求第K大时，如果使用大根堆，则要对整个数组进行建堆（整体排序）<ul>
<li>建堆完成后，根是最大值，我们要移除最大值，然后调整堆，此时根（最大值）为实际第2大</li>
<li>循环移除-调整堆K-1次，此时的堆根即为第K大的值</li>
<li>建立大根堆，有点走过头又回头的意思，可以使用更方便的小根堆</li>
</ul>
</li>
<li>小根堆：求第K大时，我们就以K为长度建立小根堆，此时根是前K个数中最小值<ul>
<li>查看下一个值（K+1），如果小于堆根，则不考虑，如果大于根堆，则替换堆根的值</li>
<li>直到所有元素遍历完成，此时长度为K的小根堆，是数组中最大的K个数，堆根就是第K大的数</li>
</ul>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">findKthLargest</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//使用小根堆思想找第K大</span></span><br><span class="line">    <span class="comment">//执行用时：1 ms, 在所有 Java 提交中击败了99.51%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：38.9 MB, 在所有 Java 提交中击败了70.49%的用户</span></span><br><span class="line">    <span class="keyword">return</span> heapSortFind(nums,k);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//使用堆排序进行寻找第K大</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">heapSortFind</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)</span></span>&#123;</span><br><span class="line">    <span class="comment">//建立一个长度为K的堆</span></span><br><span class="line">    buildMinHead(nums,k);</span><br><span class="line">    <span class="comment">//循环替换堆顶元素，最后调整堆为前K个最大元素</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = k; i &lt; nums.length; i++)&#123;</span><br><span class="line">        <span class="keyword">if</span>(nums[i] &gt; nums[<span class="number">0</span>])&#123;</span><br><span class="line">            <span class="comment">//如果下一个元素大于根堆，就替换根堆的元素为较大的这个</span></span><br><span class="line">            <span class="keyword">int</span> temp = nums[i];</span><br><span class="line">            nums[i] = nums[<span class="number">0</span>];</span><br><span class="line">            nums[<span class="number">0</span>] = temp;</span><br><span class="line">            adjectHeap(nums,<span class="number">0</span>,k-<span class="number">1</span>); <span class="comment">//一直都是调整K个元素的堆</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums[<span class="number">0</span>];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">buildMinHead</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)</span></span>&#123;</span><br><span class="line">    <span class="comment">//对前K个元素进行建堆，形成一个长度为K的小根堆</span></span><br><span class="line">    <span class="keyword">int</span> len = k-<span class="number">1</span>; <span class="comment">//0 -- k-1</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = k/<span class="number">2</span>; i &gt;= <span class="number">0</span>; i--)&#123;</span><br><span class="line">        adjectHeap(nums,i,nums.length-<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="comment">//从l到r，这一段序列中，判断l位置数字是否合适（调整堆，只能调整当前元素是否在确定的位置）</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">adjectHeap</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> l, <span class="keyword">int</span> r)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(l &gt;= r) <span class="keyword">return</span>;</span><br><span class="line">    <span class="comment">//是非叶子节点，才需要判断是否需要下沉</span></span><br><span class="line">    <span class="keyword">while</span>(l*<span class="number">2</span>+<span class="number">1</span> &lt;= r)&#123;</span><br><span class="line">        <span class="keyword">int</span> min = l;</span><br><span class="line">        <span class="keyword">if</span>(l*<span class="number">2</span>+<span class="number">1</span> &lt;= r &amp;&amp; nums[l*<span class="number">2</span>+<span class="number">1</span>] &lt; nums[min]) min = l*<span class="number">2</span>+<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span>(l*<span class="number">2</span>+<span class="number">2</span> &lt;= r &amp;&amp; nums[l*<span class="number">2</span>+<span class="number">2</span>] &lt; nums[min]) min = l*<span class="number">2</span>+<span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(min != l)&#123;</span><br><span class="line">            <span class="keyword">int</span> temp = nums[min];</span><br><span class="line">            nums[min] = nums[l];</span><br><span class="line">            nums[l] = temp;</span><br><span class="line">            l = min;    <span class="comment">// 跟踪下沉的元素，判断是否仍然需要下沉</span></span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">break</span>;    <span class="comment">//没有交换时，直接跳出循环</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Algorithm</category>
      </categories>
      <tags>
        <tag>Algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>旋转数组（字符串）</title>
    <url>/rotated-array/</url>
    <content><![CDATA[<p>旋转数组相关</p>
<a id="more"></a>

<h1 id="旋转数组（字符串）"><a href="#旋转数组（字符串）" class="headerlink" title="旋转数组（字符串）"></a>旋转数组（字符串）</h1><blockquote>
<p>算法中有关旋转数组或旋转字符串类算法汇总，思想汇总</p>
</blockquote>
<h2 id="旋转数组"><a href="#旋转数组" class="headerlink" title="旋转数组"></a><strong>旋转数组</strong></h2><blockquote>
<p>题目：给定一个数组，将数组中的元素向右移动k个位置，其中k是非负数。 <a href="https://leetcode-cn.com/problems/rotate-array/">力扣题目189：旋转数组</a> </p>
<p>示例1：</p>
<img src="/imgs/rotated-array/image-20201126153031806.png" alt="示例1" style="zoom:80%;" /> 

<p>示例2：</p>
<p><img src="/imgs/rotated-array/image-20201126153047080.png"> </p>
<p>Tips：算法至少有三种不同的方法求解，尝试使用空间复杂度为O(1)的原地算法。</p>
</blockquote>
<h3 id="方法1：暴力法求解（大循环嵌套小循环）"><a href="#方法1：暴力法求解（大循环嵌套小循环）" class="headerlink" title="方法1：暴力法求解（大循环嵌套小循环）"></a>方法1：暴力法求解（大循环嵌套小循环）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//暴力法旋转</span></span><br><span class="line"><span class="comment">//执行用时：233 ms, 在所有 Java 提交中击败了29.45%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：39.1 MB, 在所有 Java 提交中击败了72.73%的用户</span></span><br><span class="line"><span class="comment">//取模优化后：</span></span><br><span class="line"><span class="comment">//执行用时：234 ms, 在所有 Java 提交中击败了28.15%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：38.9 MB, 在所有 Java 提交中击败了89.28%的用户</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] moveRightK(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)&#123;</span><br><span class="line">    <span class="comment">//循环嵌套时，内部循环将数组右移一位，</span></span><br><span class="line">    <span class="comment">//外层循环k次，就完成了数组移动k个位置</span></span><br><span class="line">    <span class="keyword">if</span>(nums.length == <span class="number">0</span> || k == <span class="number">0</span>) <span class="keyword">return</span> nums;</span><br><span class="line">    <span class="comment">//可以用k对数组长度取模，减少不必要的循环次数</span></span><br><span class="line">    <span class="keyword">int</span> count = k % nums.length;	<span class="comment">//真正的移动次数</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; count; i++)&#123;</span><br><span class="line">        <span class="comment">//记录最后一个位置的数值，</span></span><br><span class="line">        <span class="comment">//最后一个数字，放置在起点，完成一次移动</span></span><br><span class="line">        <span class="keyword">int</span> temp = nums[nums.length-<span class="number">1</span>];</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = nums.length-<span class="number">1</span>; j &gt; <span class="number">0</span>; j--)&#123;</span><br><span class="line">            nums[j] = nums[j-<span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        nums[<span class="number">0</span>] = temp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="方法2：波浪式旋转-法（记录总移动次数）"><a href="#方法2：波浪式旋转-法（记录总移动次数）" class="headerlink" title="方法2：波浪式旋转 法（记录总移动次数）"></a>方法2：波浪式旋转 法（记录总移动次数）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//波浪式旋转（固定总移动次数，多次连续旋转）</span></span><br><span class="line"><span class="comment">//执行用时：1 ms, 在所有 Java 提交中击败了56.10%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：39.1 MB, 在所有 Java 提交中击败了74.65%的用户</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] moveRightK(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)&#123;</span><br><span class="line">    <span class="keyword">if</span>(nums.length == <span class="number">0</span> || k == <span class="number">0</span>) <span class="keyword">return</span> nums;</span><br><span class="line">    <span class="comment">//循环进行连续旋转，每次连续旋转（移动）至回到原点为止，</span></span><br><span class="line">    <span class="comment">//总旋转次数等于数组长度</span></span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;  <span class="comment">//记录总的移动次数</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; nums.length; i++)&#123;</span><br><span class="line">        <span class="keyword">int</span> cur = i;    <span class="comment">//连续旋转过程中不断变化的位置</span></span><br><span class="line">        <span class="keyword">int</span> pre = nums[i];  <span class="comment">//记录上一位置的数值</span></span><br><span class="line">        <span class="comment">//先进行旋转再判断，因此使用do while</span></span><br><span class="line">        <span class="keyword">do</span>&#123;</span><br><span class="line">            cur = (cur+k) % nums.length;</span><br><span class="line">            <span class="keyword">int</span> temp = nums[cur];</span><br><span class="line">            nums[cur] = pre;</span><br><span class="line">            pre = temp;</span><br><span class="line">            count++;</span><br><span class="line">        &#125;<span class="keyword">while</span>(cur != i);</span><br><span class="line">        <span class="keyword">if</span>(count &gt;= nums.length) <span class="keyword">break</span>;	<span class="comment">//移动次数到了就退出</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="方法3：额外数组法"><a href="#方法3：额外数组法" class="headerlink" title="方法3：额外数组法"></a>方法3：额外数组法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//额外数组使用</span></span><br><span class="line"><span class="comment">//执行用时：1 ms, 在所有 Java 提交中击败了56.10%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：39.1 MB, 在所有 Java 提交中击败了71.96%的用户</span></span><br><span class="line"><span class="comment">//优化后提交：模计算很耗性能，两次取模运算进行合并</span></span><br><span class="line"><span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：38.9 MB, 在所有 Java 提交中击败了85.94%的用户</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] moveRightK(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)&#123;</span><br><span class="line">    <span class="comment">//因为要对数组本身进行修改，直接复制出一个相同数组作为参照</span></span><br><span class="line">    <span class="keyword">if</span>(nums.length == <span class="number">0</span> || k == <span class="number">0</span>) <span class="keyword">return</span> nums;</span><br><span class="line">    <span class="keyword">int</span>[] newArr = nums.clone();</span><br><span class="line">    <span class="comment">//根据复制出来的数组的值，直接在原数组中进行相应位置的覆盖即可</span></span><br><span class="line">    <span class="comment">//int count = k % nums.length;//真实移动的长度（可优化）</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; nums.length; i++)&#123;</span><br><span class="line">        <span class="keyword">int</span> newIndex = (i+k) % nums.length;<span class="comment">//总和一次取模</span></span><br><span class="line">        nums[newIndex] = newArr[i];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h3 id="方法4：耍杂技法（三次反转，原地算法，空间O-1-）"><a href="#方法4：耍杂技法（三次反转，原地算法，空间O-1-）" class="headerlink" title="方法4：耍杂技法（三次反转，原地算法，空间O(1)）"></a>方法4：耍杂技法（三次反转，原地算法，空间O(1)）</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//耍杂技（最帅、最高端、最效率的方法）</span></span><br><span class="line"><span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：39.3 MB, 在所有 Java 提交中击败了47.26%的用户</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] moveRightK(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> k)&#123;</span><br><span class="line">    <span class="keyword">if</span>(nums.length == <span class="number">0</span> || k == <span class="number">0</span>) <span class="keyword">return</span> nums;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//数组向右移动k次，因为是循环移动的，等价于：</span></span><br><span class="line">    <span class="comment">//先总体反转，再前k个数字反转，后n-k个数字反转</span></span><br><span class="line">    <span class="comment">//具体为什么，有时间再聊了</span></span><br><span class="line">    k = k % nums.length;    <span class="comment">//保证k是一个小于数组长度的值</span></span><br><span class="line">    reverseArr(nums,<span class="number">0</span>,nums.length-<span class="number">1</span>);</span><br><span class="line">    reverseArr(nums,<span class="number">0</span>,k-<span class="number">1</span>);</span><br><span class="line">    reverseArr(nums,k,nums.length-<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> nums;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//进行数组的反转（制定起始位置的）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">int</span>[] reverseArr(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> start, <span class="keyword">int</span> end)&#123;</span><br><span class="line">    <span class="comment">//数组反转，使用双指针进行交换</span></span><br><span class="line">    <span class="keyword">while</span>(start &lt; end)&#123;</span><br><span class="line">        <span class="keyword">int</span> temp = nums[start];</span><br><span class="line">        nums[start] = nums[end];</span><br><span class="line">        nums[end] = temp;</span><br><span class="line">        start++;</span><br><span class="line">        end--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="寻找旋转排序数组中的最小值"><a href="#寻找旋转排序数组中的最小值" class="headerlink" title="寻找旋转排序数组中的最小值"></a>寻找旋转排序数组中的最小值</h2><blockquote>
<p>假设按照升序排序的数组在预先未知的某个点上进行了旋转。例如，数组[0,1,2,4,5,6,7]可能变成[4,5,6,7,0,1,2]，请找出数组中的最小元素。</p>
<p><a href="https://leetcode-cn.com/problems/find-minimum-in-rotated-sorted-array/">力扣153：寻找旋转排序数组中的最小值</a> </p>
<p>示例1：</p>
<img src="/imgs/rotated-array/image-20201127130007300.png" alt="image-20201127130007300" style="zoom:80%;" /> 

<p>示例2：</p>
<img src="/imgs/rotated-array/image-20201127130017602.png" alt="image-20201127130017602" style="zoom:80%;" /> 

<p>示例3：</p>
<img src="/imgs/rotated-array/image-20201127130030686.png" alt="image-20201127130030686" style="zoom:80%;" /> 

<p>Tips：寻找数组内部最小值，内部元素进行比较判断，使用二分法。</p>
<ul>
<li><p>（元素不重复判断比较容易）</p>
</li>
<li><p>先判断整体l-r是否有序，有序说明左侧端点为最小，返回</p>
</li>
<li><p><strong>以左侧端点为准方法：</strong>满足l &lt; r 时，取中间位置mid的值，与左侧端点值进行比较</p>
<ul>
<li>如果大于<strong>等于</strong>左侧端点，表示左侧部分有序，右侧部分无序，只考虑左侧部分<ul>
<li>因为mid=(l+r)/2，即mid有可能和左侧位置重合，此时要去右侧寻找（左侧已经判断mid）</li>
</ul>
</li>
<li>如果小于左侧端点，表示左侧无序，则最小值一定在左侧，只考虑左侧即可</li>
<li>最后当l=r时，如果到了这一步，说明当前值为最小值了</li>
</ul>
</li>
<li><p><strong>以右侧端点为准方法：</strong>同样在满足l&lt;r时，取中间位置mid的值，与右侧端点值进行比较</p>
<ul>
<li>如果小于右侧端点，说明右侧为有序，<strong>此时只能说明 l-mid 是无序的，因此要带着（mid值）</strong><ul>
<li>有可能mid点正好落在最小值上，如果r=mid-1，则失去了最小值，</li>
<li>这种情况可以在优化时提前判断mid是否为最小值来避免</li>
</ul>
</li>
<li>如果大于右端点，说明右侧无序，且mid肯定不是最小，因此l=mid+1</li>
<li>最后当l=r时，到了这一步，说明当前就是最小值</li>
</ul>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//寻找旋转数组中的最小值（数组元素于不重复）</span></span><br><span class="line"><span class="comment">//以左端点为标准</span></span><br><span class="line"><span class="comment">//如果整体有序，则左端点为最小值，返回</span></span><br><span class="line"><span class="comment">//如果左边部分有序，说明最小值在右边</span></span><br><span class="line"><span class="comment">//如果左边部分无序，说明在左边</span></span><br><span class="line"><span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：37.7 MB, 在所有 Java 提交中击败了90.18%的用户</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">findMin</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> l = <span class="number">0</span>, r = nums.length-<span class="number">1</span>;</span><br><span class="line">    <span class="comment">//不加l=r的情况，作为最后一种，直接输出l的值</span></span><br><span class="line">    <span class="keyword">while</span>(l &lt; r)&#123;</span><br><span class="line">        <span class="keyword">if</span>(nums[l] &lt; nums[r]) <span class="keyword">return</span> nums[l];   <span class="comment">//整体有序，返回左端点值</span></span><br><span class="line">        </span><br><span class="line">        <span class="keyword">int</span> mid = (l+r) / <span class="number">2</span>;</span><br><span class="line">        <span class="comment">//当前值下降，返回当前值</span></span><br><span class="line">        <span class="keyword">if</span>(mid &gt; <span class="number">0</span> &amp;&amp; nums[mid] &lt; nums[mid-<span class="number">1</span>]) <span class="keyword">return</span> nums[mid];    </span><br><span class="line">        <span class="keyword">if</span>(mid &lt; nums.length-<span class="number">1</span> &amp;&amp; nums[mid] &gt; nums[mid+<span class="number">1</span>]) <span class="keyword">return</span> nums[mid+<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(nums[mid] &gt;= nums[l]) l = mid+<span class="number">1</span>;  <span class="comment">//左侧有序，在右边 ,有可能mid=l，但是不会有mid=r </span></span><br><span class="line">        <span class="keyword">else</span> r = mid-<span class="number">1</span>;                     <span class="comment">//右侧无序，在左边</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums[l];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//从右侧开始判断</span></span><br><span class="line"><span class="comment">//如果中值小于右端点，右侧有序，则最小值在左侧，此时包括中值（即中值也可能是最小值）r=mid，</span></span><br><span class="line"><span class="comment">//（可添加判断mid是否最小值消去）</span></span><br><span class="line"><span class="comment">//如果中值大于右端点，则最小值在右侧，且中值不可能是最小l=mid+1</span></span><br><span class="line"><span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line"><span class="comment">//内存消耗：37.9 MB, 在所有 Java 提交中击败了76.68%的用户</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">findMin2</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> l = <span class="number">0</span>, r = nums.length-<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(l &lt; r)&#123;</span><br><span class="line">        <span class="keyword">if</span>(nums[l] &lt; nums[r]) <span class="keyword">return</span> nums[l];   <span class="comment">//整体有序，左端点即为最小</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> mid = (l+r) / <span class="number">2</span>;</span><br><span class="line">        <span class="comment">//提前判断，减少搜索次数</span></span><br><span class="line">        <span class="keyword">if</span>(mid &gt; <span class="number">0</span> &amp;&amp; nums[mid-<span class="number">1</span>] &gt; nums[mid]) <span class="keyword">return</span> nums[mid];</span><br><span class="line">        <span class="keyword">if</span>(mid &lt; nums.length-<span class="number">1</span> &amp;&amp; nums[mid] &gt; nums[mid+<span class="number">1</span>]) <span class="keyword">return</span> nums[mid+<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(nums[mid] &gt; nums[r]) l = mid+<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">else</span> r = mid-<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums[l];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>





<h2 id="寻找旋转排序数组中的最小值Ⅱ（数组中数可重复）"><a href="#寻找旋转排序数组中的最小值Ⅱ（数组中数可重复）" class="headerlink" title="寻找旋转排序数组中的最小值Ⅱ（数组中数可重复）"></a>寻找旋转排序数组中的最小值Ⅱ（数组中数可重复）</h2><blockquote>
<p>题目：假设按照升序排序的数组在预先未知的某个点上进行了旋转。如数组[0,1,2,4,5,6,7]可能变成[4,5,6,7,0,1,2]，请找出其中最小的元素。<strong>（注意：数组中可能存在重复的元素）</strong></p>
<p><a href="https://leetcode-cn.com/problems/find-minimum-in-rotated-sorted-array-ii/">力扣154：寻找旋转排序数组中的最小值Ⅱ（困难？？）</a> </p>
<p><a href="https://leetcode-cn.com/problems/xuan-zhuan-shu-zu-de-zui-xiao-shu-zi-lcof/">剑指Offer11：旋转数组的最小数字（简单√）</a> </p>
<p>示例1：</p>
<img src="/imgs/rotated-array/image-20201127134950375.png" alt="image-20201127134950375" style="zoom:80%;" /> 

<p>示例2：</p>
<img src="/imgs/rotated-array/image-20201127135000067.png" alt="image-20201127135000067" style="zoom:80%;" /> 

<p>Tips：数组中元素可以重复，则当mid与端点值相等时，不能判断当前段内元素是否有序，可能<strong>先降后增</strong></p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">findMin</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//寻找数组中的最小值（元素可重复）</span></span><br><span class="line">    <span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：38.3 MB, 在所有 Java 提交中击败了78.46%的用户</span></span><br><span class="line">    <span class="keyword">int</span> l = <span class="number">0</span>, r = nums.length-<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(l &lt; r)&#123;</span><br><span class="line">        <span class="keyword">if</span>(nums[l] &lt; nums[r]) <span class="keyword">return</span> nums[l];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">int</span> m = (l+r) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span>(m &gt; <span class="number">0</span> &amp;&amp; nums[m-<span class="number">1</span>] &gt; nums[m]) <span class="keyword">return</span> nums[m];</span><br><span class="line">        <span class="keyword">if</span>(m &lt; nums.length-<span class="number">1</span> &amp;&amp; nums[m] &gt; nums[m+<span class="number">1</span>]) <span class="keyword">return</span> nums[m+<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span>(nums[m] &gt; nums[l]) l = m+<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(nums[m] &lt; nums[l]) r = m-<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">else</span> l++;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> nums[l];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="搜索旋转数组（在数组中搜索目标数）"><a href="#搜索旋转数组（在数组中搜索目标数）" class="headerlink" title="搜索旋转数组（在数组中搜索目标数）"></a>搜索旋转数组（在数组中搜索目标数）</h2><blockquote>
<p>题目：给你一个整数数组<code>nums</code>，和一个整数target，该整数数组原本是按照升序排列，但输入时在某一个点上进行了数组的旋转。如数组[0,1,2,3,4,5,6,7]可能变成了[4,5,6,7,0,1,2,3]。</p>
<p><strong>问题</strong>：<strong>请你在数组中搜索target，蜀国数组中存在这个目标值，则返回它的索引，否则返回-1</strong>。</p>
<p><a href="https://leetcode-cn.com/problems/search-in-rotated-sorted-array/">力扣33：搜索旋转排序数组</a> </p>
<p>示例1：</p>
<img src="/imgs/rotated-array/image-20201126165439704.png" alt="image-20201126165439704" style="zoom:80%;" /> 

<p>示例2：</p>
<img src="/imgs/rotated-array/image-20201126165450168.png" alt="image-20201126165450168" style="zoom:80%;" /> 

<p>示例3：</p>
<img src="/imgs/rotated-array/image-20201126165503080.png" alt="image-20201126165503080" style="zoom:80%;" /> 

<p>Tips：使用二分法思想查找数</p>
<ul>
<li>因为是有序数组旋转而来，那么使用二分时，一定有一部分有序<ul>
<li>如果左边有序，且目标值在左边范围内，则考虑左边部分，<ul>
<li>否则考虑右边部分</li>
</ul>
</li>
<li>如果左边无序（右边有序），且目标值在右边范围内，则考虑右边部分，<ul>
<li>否则考虑左边部分</li>
</ul>
</li>
</ul>
</li>
<li>（根据两边有序的那一部分进行判断，因为有序的部分只需要比较就可以判断出目标值是否在其中，若不在则去另外的部分查找）</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">search</span> <span class="params">(<span class="keyword">int</span>[] A, <span class="keyword">int</span> target)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> l = <span class="number">0</span>, r = A.length-<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(l &lt;= r)&#123;</span><br><span class="line">        <span class="keyword">int</span> mid = (l+r) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span>(A[mid] == target) <span class="keyword">return</span> mid;</span><br><span class="line">        <span class="keyword">if</span>(A[l] &lt;= A[mid])&#123;</span><br><span class="line">            <span class="comment">//左边有序</span></span><br><span class="line">            <span class="keyword">if</span>(target &gt;= A[l] &amp;&amp; target &lt; A[mid])&#123;</span><br><span class="line">                r = mid-<span class="number">1</span>;</span><br><span class="line">            &#125;<span class="keyword">else</span> l = mid+<span class="number">1</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">//右边有序</span></span><br><span class="line">            <span class="keyword">if</span>(target &gt; A[mid] &amp;&amp; target &lt;= A[r])&#123;</span><br><span class="line">                l = mid+<span class="number">1</span>;</span><br><span class="line">            &#125;<span class="keyword">else</span> r = mid-<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="搜索旋转数组2（在旋转后的数组中搜索可重复的目标）"><a href="#搜索旋转数组2（在旋转后的数组中搜索可重复的目标）" class="headerlink" title="搜索旋转数组2（在旋转后的数组中搜索可重复的目标）"></a>搜索旋转数组2（在旋转后的数组中搜索可重复的目标）</h2><blockquote>
<p>题目：给定一个排序后的数组，包含n个整数，但这个数组已经被旋转过很多次了，次数不详。请编写代码找出数组中的某个元素，假设数组元素是按照升序排列的。若有多个相同元素，返回索引值最小的。</p>
<p><a href="https://leetcode-cn.com/problems/search-in-rotated-sorted-array-ii/">力扣81：搜索旋转排序数组Ⅱ</a>  <a href="https://leetcode-cn.com/problems/search-rotate-array-lcci/">力扣1825：面试题10.03.搜索旋转数组</a>   </p>
<p>示例1：</p>
<img src="/imgs/rotated-array/image-20201127094304993.png" alt="image-20201127094304993" style="zoom:80%;" /> 

<p>示例2：</p>
<img src="/imgs/rotated-array/image-20201127094316243.png" alt="image-20201127094316243" style="zoom:80%;" /> 

<p>Tips：和搜索旋转数组1不同的地方是当前问题中数字可重复，在判断左右是否有序时充分考虑中值与端点值<strong>相等时的情况</strong>。</p>
</blockquote>
<p>解题思路：</p>
<ul>
<li>此题目添加了数组中数字可以重复，且目标值重复是取下标最小的（这个怎么找呢？）</li>
<li>数组中允许重复数字时，判断部分是否有序的标准就发生了变化，好像就不能那么用了，即中值与端点值相等时，不能判断是否为有序的，要单独考虑</li>
<li>思路步骤：只考虑左侧部分的所有情况，<ul>
<li>如果中值等于目标值，则判断前一位是否相等，不相等时说明当前索引为最小，否则，</li>
<li>中值明确大于左端点值时，左侧明确有序，可判断目标值是否在该有序段内，<ul>
<li>在左侧则只考虑左侧</li>
<li>否则一定在右侧</li>
</ul>
</li>
<li>中值明确小于左端点值时，左侧明确无序，则右侧一定有序，判断目标值是否在右侧段<ul>
<li>在右侧则只考虑右侧</li>
<li>否则一定在左侧</li>
</ul>
</li>
<li>相等，不知道是否有序，移动端点后判断，即左端点+1，重新获取中值进行判断</li>
</ul>
</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">search</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> target)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//二分法在旋转的有序数组中搜索目标数</span></span><br><span class="line">    <span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：39.6 MB, 在所有 Java 提交中击败了65.54%的用户</span></span><br><span class="line">    <span class="keyword">if</span>(arr.length == <span class="number">0</span>) <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">if</span>(arr[<span class="number">0</span>] == target) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">int</span> l = <span class="number">0</span>, r = arr.length-<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(l &lt;= r)&#123;</span><br><span class="line">        <span class="keyword">int</span> mid = (l + r) / <span class="number">2</span>;</span><br><span class="line">        <span class="comment">//中值就是目标值是，查看前一位是否相等，不相等时返回当前索引为最小</span></span><br><span class="line">        <span class="keyword">if</span>(arr[mid] == target)&#123;</span><br><span class="line">            <span class="keyword">while</span>(mid &gt; <span class="number">0</span> &amp;&amp; arr[mid-<span class="number">1</span>] == target) mid--;</span><br><span class="line">            <span class="keyword">return</span> mid;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(arr[l] &lt; arr[mid])&#123;</span><br><span class="line">            <span class="comment">//中值明确大于左端点时，左边是有序的</span></span><br><span class="line">            <span class="keyword">if</span>(target &gt;= arr[l] &amp;&amp; target &lt; arr[mid]) r = mid-<span class="number">1</span>;</span><br><span class="line">            <span class="keyword">else</span> l = mid+<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(arr[l] &gt; arr[mid])&#123;</span><br><span class="line">            <span class="comment">//中值明确小于左端点时，左边是无序的，则右侧一定有序了</span></span><br><span class="line">            <span class="keyword">if</span>(target &gt; arr[mid] &amp;&amp; target &lt;= arr[r]) l = mid+<span class="number">1</span>;</span><br><span class="line">            <span class="keyword">else</span> r = mid-<span class="number">1</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">//中值和左端点相等时，移动左端点，继续判断</span></span><br><span class="line">            l++;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="旋转链表"><a href="#旋转链表" class="headerlink" title="旋转链表"></a><strong>旋转链表</strong></h2><blockquote>
<p>题目：给定一个链表，旋转链表，将链表每个结点向右移动k个位置，其中k是非负数。</p>
<p><a href="https://leetcode-cn.com/problems/rotate-list/">力扣61：旋转链表</a> </p>
<p>示例1：</p>
<img src="/imgs/rotated-array/image-20201127155923980.png" alt="image-20201127155923980" style="zoom:80%;" /> 

<p>示例2：</p>
<img src="/imgs/rotated-array/image-20201127155934300.png" alt="image-20201127155934300" style="zoom:80%;" /> 

<p>Tips：旋转链表分三步</p>
<ul>
<li>遍历一次，获取链表长度（同时获取到链表的最后结点，指向原头结点，形成环）</li>
<li>根据移动长度k计算断开链表的位置 n - k % n</li>
<li>遍历到断开链表的前置结点，赋值头结点并将前置结点指向null</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> ListNode <span class="title">rotateRight</span><span class="params">(ListNode head, <span class="keyword">int</span> k)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//先获取链表长度，用移动长度k对链表长度取模，得到真实移动长度</span></span><br><span class="line">    <span class="comment">//使用指针移动到真实长度处，将链表解开，作为新链表头部，并把原链表头部接到尾部</span></span><br><span class="line">    <span class="comment">//记录头结点，记录尾结点</span></span><br><span class="line">    <span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：37.9 MB, 在所有 Java 提交中击败了74.89%的用户</span></span><br><span class="line">    <span class="keyword">if</span>(head == <span class="keyword">null</span> || head.next == <span class="keyword">null</span>) <span class="keyword">return</span> head;</span><br><span class="line">    <span class="comment">//获取链表长度，及尾结点</span></span><br><span class="line">    ListNode p = head;</span><br><span class="line">    ListNode end = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">while</span>(p.next != <span class="keyword">null</span>)&#123;</span><br><span class="line">        p = p.next;</span><br><span class="line">        count++;</span><br><span class="line">    &#125;</span><br><span class="line">    end = p;    <span class="comment">//获取尾结点(此处也可以将链表成环)</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span>(k % count == <span class="number">0</span>) <span class="keyword">return</span> head;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//找到需要解链的位置,移动一位，则解开n-1处的链，解开n-1的链，则要找到n-2处结点（前置节点）</span></span><br><span class="line">    <span class="keyword">int</span> num = count - (k % count);</span><br><span class="line">    p = head;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; num-<span class="number">1</span>; i++)&#123;</span><br><span class="line">        p = p.next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//尾结点 -&gt; 头结点</span></span><br><span class="line">    <span class="comment">//头结点 -&gt; 前置节点的下一节点</span></span><br><span class="line">    <span class="comment">//前置结点置空，作为新链的尾结点</span></span><br><span class="line">    end.next = head;</span><br><span class="line">    head = p.next;</span><br><span class="line">    p.next = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="旋转字符串（左旋）"><a href="#旋转字符串（左旋）" class="headerlink" title="旋转字符串（左旋）"></a><strong>旋转字符串（左旋）</strong></h2><blockquote>
<p>字符串的左旋操作是把字符串前面的若干个字符转移到字符串的尾部。请定义一个函数实现字符串左旋操作的功能。比如，输入字符串<code>“abcdefg”</code>和数字2，该函数将返回左旋两位得到的结果<code>&quot;cdefgab&quot;</code>。</p>
<p><a href="https://leetcode-cn.com/problems/zuo-xuan-zhuan-zi-fu-chuan-lcof/">剑指Offer58-Ⅱ：左旋转字符串</a> </p>
<p>示例1：</p>
<img src="/imgs/rotated-array/image-20201127162515364.png" alt="image-20201127162515364" style="zoom:80%;" /> 

<p>示例2：</p>
<img src="/imgs/rotated-array/image-20201127162526236.png" alt="image-20201127162526236" style="zoom:80%;" /> 

<p>Tips： Java 中字符串是不可变的，不能像数组那样使用原地多次反转来实现旋转，字符串要使用空间</p>
<ul>
<li>根据字符串长度获取实际需要移动长度，字符串相应位置即变成第一个字符</li>
<li>使用新的字符串从新字符串头部加到字符串结束，然后再从头开始添加，循环一周即可<ul>
<li>可以使用取余方法将两个循环合并成一个</li>
</ul>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">reverseLeftWords</span><span class="params">(String s, <span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//字符串左旋操作</span></span><br><span class="line">    <span class="comment">//查找位置，进行两部分的遍历拼接，返回新字符串</span></span><br><span class="line">    <span class="comment">//执行用时：5 ms, 在所有 Java 提交中击败了21.60%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：38.3 MB, 在所有 Java 提交中击败了74.95%的用户</span></span><br><span class="line">    <span class="keyword">int</span> index = n % s.length();</span><br><span class="line">    <span class="keyword">if</span>(index == <span class="number">0</span>) <span class="keyword">return</span> s;</span><br><span class="line"></span><br><span class="line">    StringBuffer sb = <span class="keyword">new</span> StringBuffer();</span><br><span class="line">    <span class="comment">// for(int i = index; i &lt; s.length(); i++)&#123;</span></span><br><span class="line">    <span class="comment">//     //char c = s.charAt(i);</span></span><br><span class="line">    <span class="comment">//     sb.append(s.charAt(i));</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// for(int i = 0; i &lt; index; i++)&#123;</span></span><br><span class="line">    <span class="comment">//     //char c = s.charAt(i);</span></span><br><span class="line">    <span class="comment">//     sb.append(s.charAt(i));</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">//可以用取余操作将两个循环合并</span></span><br><span class="line">    <span class="comment">//执行用时：7 ms, 在所有 Java 提交中击败了13.38%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：38.1 MB, 在所有 Java 提交中击败了88.72%的用户</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = index; i &lt; s.length() + index; i++)&#123;</span><br><span class="line">        sb.append(s.charAt(i%s.length()));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> sb.toString();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="旋转字符串（旋转匹配）"><a href="#旋转字符串（旋转匹配）" class="headerlink" title="旋转字符串（旋转匹配）"></a><strong>旋转字符串（旋转匹配）</strong></h2><blockquote>
<p>题目：给定两个字符串A和B，A的旋转操作就是将A最左边的字符移动到最右边，例如，若A=<code>abcd</code>，在移动一次后结果就是<code>bcda</code>。如果在若干次旋转操作之后，A能变成B，就说明A可以旋转成B，返回True。</p>
<p><a href="https://leetcode-cn.com/problems/rotate-string/">力扣796：旋转字符串</a> </p>
<p><a href="https://leetcode-cn.com/problems/string-rotation-lcci/">力扣 面试题01.09：字符串轮转</a> </p>
<img src="/imgs/rotated-array/image-20201127170623444.png" alt="image-20201127170623444" style="zoom:80%;" /> 

<p>Tips：这个题可以转换成字符串匹配的题目</p>
<ul>
<li>遍历方法<ul>
<li>遍历A字符串所有可能的旋转字符串，与B进行比较</li>
<li>在B中找到A字符串所有首字母，根据位置旋转B字符串，结果与A比较（旋转字符串思想）</li>
</ul>
</li>
<li>A+A这个长字符串包括所有A旋转后的结果，判断B是否在A+A中<ul>
<li>可以使用<code>str.contains(str)</code>判断</li>
<li>高端进阶：可以使用<code>!!KMP!!</code></li>
<li><code>KMP</code>一定要学习！，算法不学<code>KMP</code>，…..</li>
</ul>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">rotateString</span><span class="params">(String A, String B)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//旋转字符串</span></span><br><span class="line">    <span class="comment">//如果字符串相同，则true</span></span><br><span class="line">    <span class="comment">//在B字符串中寻找A字符串的首字符位置</span></span><br><span class="line">    <span class="comment">//从所有首字符位置遍历整个字符串，判断是否和A相等，相等为true</span></span><br><span class="line">    <span class="comment">//执行用时：1 ms, 在所有 Java 提交中击败了32.14%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：36.4 MB, 在所有 Java 提交中击败了78.81%的用户</span></span><br><span class="line">    <span class="keyword">if</span>(A.length() != B.length()) <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">if</span>(A.equals(B)) <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">char</span> headC = A.charAt(<span class="number">0</span>);</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; B.length(); i++)&#123;</span><br><span class="line">        <span class="keyword">char</span> cur = B.charAt(i);</span><br><span class="line">        <span class="keyword">if</span>(cur == headC &amp;&amp; getString(B,i).equals(A))&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//旋转B字符串，看看是否能够旋转回到A</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getString</span><span class="params">(String s, <span class="keyword">int</span> startIndex)</span></span>&#123;</span><br><span class="line">    StringBuffer sb = <span class="keyword">new</span> StringBuffer();</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = startIndex; i &lt; s.length() + startIndex; i++)&#123;</span><br><span class="line">        sb.append(s.charAt(i%s.length()));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sb.toString();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<h2 id="旋转图像（矩阵）"><a href="#旋转图像（矩阵）" class="headerlink" title="旋转图像（矩阵）"></a><strong>旋转图像（矩阵）</strong></h2><blockquote>
<p>题目：给定一个n X n的二维矩阵表示一个图像，将图像顺时针旋转90度。说明：你必须原地旋转图像，即需要直接修改输入的二维矩阵，不要使用另外一个矩阵来旋转图像。</p>
<p><a href="https://leetcode-cn.com/problems/rotate-image/">力扣48：旋转图像</a> </p>
<p><a href="https://leetcode-cn.com/problems/rotate-matrix-lcci/">力扣 面试题01.07：旋转矩阵</a> </p>
<p>示例1：</p>
<img src="/imgs/rotated-array/image-20201127174436929.png" alt="image-20201127174436929" style="zoom:80%;" /> 

<p>示例2：</p>
<img src="/imgs/rotated-array/image-20201127174457402.png" alt="image-20201127174457402" style="zoom:80%;" /> 

<p>Tips：观察发现，矩阵经过顺时针旋转90度后</p>
<ul>
<li>方法一：先转置，再水平反转，就是顺时针旋转90度</li>
<li>方法二：原地旋转（！很漂亮！）</li>
<li>方法三：使用辅助数组进行旋转<ul>
<li>列变成了行 即<code>newRow = col;</code></li>
<li>行变成了倒数列，即<code>newCol = (len-1) - row</code></li>
</ul>
</li>
</ul>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//方法一：</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">rotate</span><span class="params">(<span class="keyword">int</span>[][] matrix)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//旋转矩阵。</span></span><br><span class="line">    <span class="comment">//顺时针旋转90度，相当于转置矩阵（横纵坐标互换），</span></span><br><span class="line">    	<span class="comment">//再水平反转（列反转）</span></span><br><span class="line">    <span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：38.8 MB, 在所有 Java 提交中击败了41.91%的用户</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">//求转置，只循环上三角</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; matrix.length; i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = i; j &lt; matrix[<span class="number">0</span>].length; j++)&#123;</span><br><span class="line">            <span class="comment">//转置是有对称轴的，即两个点元素互换</span></span><br><span class="line">            <span class="keyword">int</span> temp = matrix[i][j];</span><br><span class="line">            matrix[i][j] = matrix[j][i];</span><br><span class="line">            matrix[j][i] = temp;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//水平反转,列循环一半</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; matrix.length; i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; matrix[<span class="number">0</span>].length/<span class="number">2</span>; j++)&#123;</span><br><span class="line">            <span class="comment">//转置是有对称轴的，即两个点元素互换</span></span><br><span class="line">            <span class="keyword">int</span> temp = matrix[i][j];</span><br><span class="line">            matrix[i][j] = matrix[i][matrix[<span class="number">0</span>].length-<span class="number">1</span>-j];</span><br><span class="line">            matrix[i][matrix[<span class="number">0</span>].length-<span class="number">1</span>-j] = temp;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//方法二：一个循环中，四个值进行交换</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">rotate</span><span class="params">(<span class="keyword">int</span>[][] matrix)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//数组旋转，</span></span><br><span class="line">    <span class="comment">//根据规律可以看出:</span></span><br><span class="line">    <span class="comment">//(i,j) -&gt; (j,n-1-i)</span></span><br><span class="line">    <span class="comment">//(j,n-1-i) -&gt; (n-1-i,n-1-j)</span></span><br><span class="line">    <span class="comment">//(n-1-i,n-1-j) -&gt; (n-1-j,i)</span></span><br><span class="line">    <span class="comment">//(n-1-j,i) -&gt; (i,j)</span></span><br><span class="line">    <span class="comment">//即，这四个点进行了循环交换，</span></span><br><span class="line">    <span class="comment">//找出当前矩阵中供有多少次交换 </span></span><br><span class="line">    <span class="comment">//n^2 / 4 （n是偶数时） n/2 * n/2；</span></span><br><span class="line">    <span class="comment">// (n^2-1) / 4（n是奇数时） n/2 * (n+1)/2</span></span><br><span class="line">    <span class="comment">//双层循环，行是n/2，列是(n+1)/2</span></span><br><span class="line">    <span class="comment">//执行用时：0 ms, 在所有 Java 提交中击败了100.00%的用户</span></span><br><span class="line">    <span class="comment">//内存消耗：38.5 MB, 在所有 Java 提交中击败了82.59%的用户</span></span><br><span class="line">    <span class="keyword">int</span> n = matrix.length;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n/<span class="number">2</span>; i++)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; (n+<span class="number">1</span>)/<span class="number">2</span>; j++)&#123;</span><br><span class="line">            <span class="comment">//逆向替换</span></span><br><span class="line">            <span class="keyword">int</span> temp = matrix[i][j];</span><br><span class="line">            matrix[i][j] = matrix[n-<span class="number">1</span>-j][i];</span><br><span class="line">            matrix[n-<span class="number">1</span>-j][i] = matrix[n-<span class="number">1</span>-i][n-<span class="number">1</span>-j];</span><br><span class="line">            matrix[n-<span class="number">1</span>-i][n-<span class="number">1</span>-j] = matrix[j][n-<span class="number">1</span>-i];</span><br><span class="line">            matrix[j][n-<span class="number">1</span>-i] = temp;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>Algorithm</category>
      </categories>
      <tags>
        <tag>Algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>linux</title>
    <url>/linux/</url>
    <content><![CDATA[<p>Linux是一种自由和开放源码的系统，存在着许多不同的Linux版本，但它们都使用了Linux内核。Linux可以安装在各种计算机硬件设备中，如手机、平板电脑、路由器、台式计算机等。</p>
<a id="more"></a>

<h1 id="Linux"><a href="#Linux" class="headerlink" title="Linux"></a>Linux</h1><h2 id="Linux系统安装"><a href="#Linux系统安装" class="headerlink" title="Linux系统安装"></a>Linux系统安装</h2><h3 id="虚拟机安装"><a href="#虚拟机安装" class="headerlink" title="虚拟机安装"></a>虚拟机安装</h3><blockquote>
<p><a href="https://blog.csdn.net/babyxue/article/details/80970526">参考教程</a> </p>
</blockquote>
<h3 id="虚拟机克隆"><a href="#虚拟机克隆" class="headerlink" title="虚拟机克隆"></a>虚拟机克隆</h3><ul>
<li>关闭当前系统</li>
<li>使用虚拟机完整克隆系统</li>
<li>进入新系统修改相关配置<ul>
<li>修改ip地址</li>
<li>修改主机名：<code>hostnamectl set-hostname &lt;新的主机名&gt;</code> </li>
<li>修改ip地址和主机名映射</li>
</ul>
</li>
</ul>
<h3 id="Linux目录结构"><a href="#Linux目录结构" class="headerlink" title="Linux目录结构"></a>Linux目录结构</h3><blockquote>
<p>Linux根目录为<code>/</code>，可以使用<code>cd /</code>进入根目录，可以使用<code>pwd</code>查看当前所在目录位置。</p>
</blockquote>
<p>根目录 <code>/</code>的结构为：</p>
<img src="/imgs/linux/image-20201217170543598.png" alt="Linux目录结构" style="zoom:80%;" />

<ul>
<li>bin：binaries，存放二进制可执行文件</li>
<li>sbin：super user binaries，存放二进制可执行文件，只有root超级用户可以访问</li>
<li>etc：etcetera，存放系统配置文件</li>
<li>usr：unix shared resources，用于存放共享的系统资源</li>
<li>home：存放用户文件的根目录</li>
<li>root：超级用户目录</li>
<li>dev：devices，用于存放设备文件</li>
<li>lib：library，存放跟文件系统中的程序运行所需要的共享库以及内核模块</li>
<li>mnt：mount，系统管理员安装临时文件系统的安装点</li>
<li>boot：存放用于系统引导时使用的各种文件</li>
<li>tmp：temporary，用于存放各种临时文件</li>
<li>var：variable，用于存放运行时需要改变数据的文件</li>
</ul>
<p>Linux系统中<code>/</code>路径下使用命令查看内容：</p>
<img src="/imgs/linux/image-20201217171828595.png" alt="使用ll查看内容" style="zoom:80%;" />

<h2 id="Linux配置"><a href="#Linux配置" class="headerlink" title="Linux配置"></a>Linux配置</h2><h3 id="网络IP设置"><a href="#网络IP设置" class="headerlink" title="网络IP设置"></a>网络IP设置</h3><blockquote>
<p><a href="https://dechen.blog.csdn.net/article/details/105407711?utm_medium=distribute.pc_relevant.none-task-blog-BlogCommendFromMachineLearnPai2-3.channel_param&depth_1-utm_source=distribute.pc_relevant.none-task-blog-BlogCommendFromMachineLearnPai2-3.channel_param">教程</a> </p>
<p>centOS7中使用ens33代替了eth0文件，即文件<code>/etc/sysconfig/network-scripts/ifcfg-ens33</code>。</p>
<p>使用时，如果需要，可以将ens33文件重命名为eth0后，更改配置，重新启动网络。</p>
</blockquote>
<ul>
<li><p>修改主机名：<code>vi /etc/sysconfig/network</code> </p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">#/etc/sysconfig/network</span><br><span class="line">NETWORKING=yes		#网络是否工作，不可以为no</span><br><span class="line">HOSTNAME=shizhan	#主机名</span><br></pre></td></tr></table></figure>
</li>
<li><p>修改ip地址和主机名的映射关系：<code>vi /etc/hosts</code> </p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">/etc/hosts</span><br><span class="line">127.0.0.1 localhost  ...</span><br><span class="line">192.168.2.2 CentOS1</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p><strong>设置IP为静态地址</strong>：</p>
<ol>
<li><p>查看虚拟机提供给系统的网段</p>
<img src="/imgs/linux/image-20201218093833425.png" alt="虚拟机网段" style="zoom:80%;" />
</li>
<li><p>修改IP地址，使用管理员权限执行命令：<code>vi /etc/sysconfig/network-scripts/ifcfg-ens33</code></p>
<figure class="highlight vim"><table><tr><td class="code"><pre><span class="line">#ifcfg-ens33</span><br><span class="line">TYPE=Ethernet</span><br><span class="line">BOOTPROTO=static	#static为静态ip，dhcp为自动获取动态ip</span><br><span class="line">DEFROUTE=yes</span><br><span class="line"></span><br><span class="line">NAME=ens33			#网卡设备名，要和文件名一致，如修改成eth0，则此处也要一致</span><br><span class="line"></span><br><span class="line">DEVICE=ens33		#网卡设备名，要和文件名一致，如修改成eth0，则此处也要一致</span><br><span class="line">ONBOOT=yes			#yes=网卡随网络服务启动  系统默认不启动，这里一定要设置成启动</span><br><span class="line"></span><br><span class="line">#默认为dhcp，修改为static时需要新增下面部分内容</span><br><span class="line">DNS1=<span class="number">114.114</span>.<span class="number">114.114</span>	#DNS服务器ip地址</span><br><span class="line">IPADDR=<span class="number">192.168</span>.<span class="number">2.10</span>		#网卡为当前系统配置的静态ip地址</span><br><span class="line">NETMASK=<span class="number">255.255</span>.<span class="number">255.0</span>	#子网掩码</span><br><span class="line">GATEWAY=<span class="number">192.168</span>.<span class="number">2.1</span>		#网关</span><br></pre></td></tr></table></figure>
</li>
<li><p> 重启网络服务：<code>service network restart</code> 或者 <code>systemtcl restart network</code> </p>
</li>
<li><p>如果需要，则关闭系统防火墙：<code>systemctl stop firewalld</code> </p>
</li>
<li><p>查看修改后的ip地址：<code>ifconfig</code> 或者 <code>ip addr show</code> </p>
<img src="/imgs/linux/image-20201218092804966.png" alt="image-20201218092804966" style="zoom:80%;" />

</li>
</ol>
<h3 id="防火墙配置"><a href="#防火墙配置" class="headerlink" title="防火墙配置"></a><strong>防火墙配置</strong></h3><blockquote>
<p>CentOS7默认采用的是firewalld管理netfilter子系统，底层调用的仍然是iptables命令。</p>
</blockquote>
<p>firewalld：dynamic firewall daemon，支持ipv4和ipv6.</p>
<ul>
<li>firewalld可以动态修改单条规则，并不需要像iptables那样，在修改了规则后必须全部刷新才可以生效。</li>
<li>firewalld在使用上要比iptables更人性化。</li>
</ul>
<img src="/imgs/linux/image-20201218112535580.png" alt="Linux防火墙结构" style="zoom:80%;" />

<p><strong>firewalld配置方式：</strong></p>
<ul>
<li>firewall-config：GUI工具</li>
<li>firewall-cmd：命令行工具<ul>
<li>firewall-cmd –version：查看版本号</li>
<li>firewall-cmd –state：查看防火墙状态</li>
<li>firewall-cmd –list-all：查看全部信息</li>
<li>firewall-cmd –list-ports：只看端口信息</li>
<li>等其他</li>
</ul>
</li>
<li>直接编辑xml文件，编辑后需要reload才会生效</li>
</ul>
<p><strong>配置文件存放位置：</strong>firewalld的配置文件以xml文件为主（主配置文件为firewalld.conf），有两个位置</p>
<ul>
<li><code>/etc/firewalld/</code> ：存放修改过的配置，优先查找，找不到才去查找默认的配置</li>
<li><code>/usr/lib/firewalld/</code>：默认的配置</li>
<li>修改配置的时候只需要将<code>/usr/lib/firewalld/</code>复制到<code>/etc/firewalld/</code> 进行修改即可。</li>
</ul>
<h4 id="防火墙相关命令："><a href="#防火墙相关命令：" class="headerlink" title="防火墙相关命令："></a><strong>防火墙相关命令：</strong></h4><ul>
<li>systemctl status firewalld：查看防火墙状态</li>
<li>systemctl stop firewalld：临时关闭防火墙</li>
<li>systemctl disable firewalld：禁止防火墙开机自启动</li>
<li>service firewalld start：开启防火墙</li>
<li><strong>service firewalld restart：重启防火墙</strong></li>
<li>service firewalld stop：关闭防火墙</li>
<li>开端口命令：firewall-cmd –zone=public –add-port=8080/tcp –permanent<ul>
<li>–zone：作用域</li>
<li>–add-port=8080/tcp：添加端口，格式即 端口/协议</li>
<li>–permanent：永久生效，不加此参数重启后失效</li>
</ul>
</li>
</ul>
<h2 id="Linux命令"><a href="#Linux命令" class="headerlink" title="Linux命令"></a>Linux命令</h2><blockquote>
<p>命令格式：命令 -选项 参数</p>
<p>如： ls -a /usr   =&gt;  显示/usr目录下所有文件信息</p>
</blockquote>
<h3 id="关机-重启命令"><a href="#关机-重启命令" class="headerlink" title="关机/重启命令"></a>关机/重启命令</h3><ul>
<li>shutdown：系统关机<ul>
<li>-r：关机后立即重启</li>
<li>-h：halt，关机后不重新启动，关闭电源</li>
</ul>
</li>
<li>reboot：重新启动，类似 shutdown -r</li>
</ul>
<h3 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h3><ul>
<li>内部命令：属于Shell解析器的一部分<ul>
<li>pwd：print working directory，显示当前工作目录</li>
<li>cd：change directory，切换目录</li>
<li>help：帮助，后跟命令，可以查看该命令的详细描述，如 help cd</li>
</ul>
</li>
<li>外部命令：独立于Shell解析器之外的文件程序<ul>
<li>ls：list，显示文件和目录列表，可以指定选项和参数，不指定默认所在目录<ul>
<li>-l：long，输入文件和目录的详细信息，和 ll 命令相同</li>
<li>-a：all，所有文件和目录，包括隐藏文件和特殊目录</li>
<li>-t：time</li>
</ul>
</li>
<li>mkdir：make directory，创建目录</li>
<li>cp：copy，复制文件或目录</li>
<li>man：manual，查看帮助文档，后跟命令，如 man ls</li>
</ul>
</li>
<li>快捷键：<ul>
<li>ctrl + c：停止当前进程</li>
<li>ctrl + r：查看命令历史</li>
<li>ctrl + l：清屏，与clear命令作用相同</li>
</ul>
</li>
</ul>
<h3 id="系统状态"><a href="#系统状态" class="headerlink" title="系统状态"></a>系统状态</h3><ul>
<li>top：显示当前系统中耗费资源最多的进程，动态列表</li>
<li>ps：显示瞬间的进程状态<ul>
<li>-e/-A：显示所有进程，环境变量</li>
<li>-f：全格式</li>
<li>-a：显示所有用户的所有进程（包括其他用户）</li>
<li>-u：按用户名和启动时间的顺序来显示进程</li>
<li>-x：显示无控制终端的进程</li>
</ul>
</li>
<li>kill：杀死一个进程<ul>
<li>kill -9 pid ：杀死指定pid的进程</li>
</ul>
</li>
<li>df：显示文件系统磁盘空间的使用情况</li>
<li>ifconfig：网卡网络配置详解</li>
<li>ping：测试网络的连通性（后跟ip或域名）</li>
<li>netstat：显示网络状态信息<ul>
<li>-a：显示所有的连接和监听端口</li>
<li>-t：tcp，仅显示tcp相关选项</li>
<li>-u：udp，仅显示udp相关选项</li>
<li>-n：拒绝显示别名，能显示数字的全部转化成数字</li>
<li>-p：显示建立相关链接的程序名</li>
</ul>
</li>
<li>free：显示当前内存和交换空间的使用情况</li>
<li>du：显示指定文件（目录）已使用的磁盘空间的总和<ul>
<li>-h：human-readable，文件大小以K，M，G为单位显示</li>
<li>-s：summarize，只显示各档案大小的总和</li>
</ul>
</li>
</ul>
<h3 id="操作文件或目录"><a href="#操作文件或目录" class="headerlink" title="操作文件或目录"></a>操作文件或目录</h3><ul>
<li>mkdir：make directory，创建目录<ul>
<li>-p：parents，创建指定目录时，如果父目录不存在则会先生父目录</li>
</ul>
</li>
<li>touch：创建空文件</li>
<li>cp：copy，复制文件或目录<ul>
<li>-r：recursive，递归处理，将指定目录下的文件与子目录一块拷贝</li>
</ul>
</li>
<li>mv：move，移动文件或目录，文件或目录改名</li>
<li><code>echo &quot;abc&quot; &gt; file3.txt</code> ：将字符串内容输出到file3文件中，没有则先创建文件<ul>
<li><strong><code>&gt;</code> ：代表覆盖内容</strong></li>
<li><strong><code>&gt;&gt;</code>：代表在文件中追加内容</strong></li>
</ul>
</li>
<li>less、more、cat，都是查看输出文件内容的命令，可以配合 <code>&gt;</code> 命令输出内容到新文件中<ol>
<li><code>cat 1.txt &gt; file4.txt</code></li>
<li><code> less 1.txt &gt; file5.txt</code> </li>
<li><code>more 1.txt &gt; file6.txt</code> </li>
</ol>
</li>
<li>rm：remove，删除文件<ul>
<li><code>-i</code>：删除前逐一询问确认</li>
<li>-r：recursive，同时删除该目录下的所有文件</li>
<li>-f：force，强制删除文件或目录（递归删除）</li>
</ul>
</li>
<li>rmdir：remove directory，删除空目录</li>
<li>cat：catenate，显示文本内容</li>
<li>more、less：分页显示文本文件内容</li>
<li>head、tail：查看文本中开头或结尾部分的内容<ul>
<li>head -n 5 a.log：查看a.log文件的前5行</li>
<li>tail -F b.log：follow，循环读取文件内容</li>
</ul>
</li>
<li>wc：word count，统计文本的行数、字数、字符数<ul>
<li>-m：统计文本字符数</li>
<li>-w：统计文本字数</li>
<li>-l：统计文本行数</li>
</ul>
</li>
<li>find：在文件系统中查找指定的文件，<code>find/etc/ -name &quot;aaa&quot;</code>：在/etc/目录下查找名为aaa的文件</li>
<li>grep：在指定的文本文件中查找指定的字符串</li>
</ul>
<h3 id="压缩-解压命令"><a href="#压缩-解压命令" class="headerlink" title="压缩/解压命令"></a>压缩/解压命令</h3><ul>
<li>gzip [参数项] 文件名：压缩/解压文件或目录，压缩文件后缀名为gz<ul>
<li>-d：decompress，将压缩文件解压</li>
<li>-l：list，显示压缩文件的大小，未压缩文件的大小，压缩比</li>
<li>-v：verbose，显示文件名和压缩比</li>
<li>-num：用指定的数字num来调整压缩的速度，如：<ul>
<li>-1或–fase表示最快压缩方法，低压缩比</li>
<li>-9或–best表示最慢压缩方法，高压缩比</li>
<li>系统缺省值（默认）为6</li>
</ul>
</li>
</ul>
</li>
<li>bzip2  [参数项] 文件名：压缩/解压文件或目录，压缩文件后缀为bz2<ul>
<li>-z：compress，压缩的参数</li>
<li>-d：decompress，解压缩的参数</li>
<li>-c：将压缩过程产生的数据输出到屏幕上</li>
<li>-num：同gzip中数字功能</li>
</ul>
</li>
<li>tar  [参数项] 文件名：文件、目录打包/解包<ul>
<li>-c：create，建立一个压缩文件的参数指令</li>
<li>-x：extract，解开一个压缩文件的参数指令</li>
<li>-z：是否需要用gzip压缩</li>
<li>-j：是否使用bzip2压缩</li>
<li>-v：verbose，压缩过程中显示文件</li>
<li>-f：使用档名，在f之后要立即接档名（file）</li>
</ul>
</li>
</ul>
<h2 id="Vim编辑器"><a href="#Vim编辑器" class="headerlink" title="Vim编辑器"></a>Vim编辑器</h2><h3 id="Vim工作模式"><a href="#Vim工作模式" class="headerlink" title="Vim工作模式"></a>Vim工作模式</h3><img src="/imgs/linux/image-20201218170322451.png" alt="vim工作模式" style="zoom:67%;" />



<h3 id="定位命令"><a href="#定位命令" class="headerlink" title="定位命令"></a>定位命令</h3><ul>
<li>:set nu：显示行号</li>
<li>:set nonu：取消行号</li>
<li>gg：小写，定位到文本的第一行</li>
<li>G：大写，定位到文本的最后一行</li>
<li>:n：定位到文本的第n行</li>
</ul>
<h3 id="插入命令"><a href="#插入命令" class="headerlink" title="插入命令"></a>插入命令</h3><ul>
<li>i：小写，在光标<strong>前</strong>插入内容</li>
<li>I：大写，在光标当前行<strong>开始处</strong>插入内容</li>
<li>a：小写，在光标<strong>后</strong>插入内容</li>
<li>A：大写，在光标当前行<strong>末尾处</strong>插入内容</li>
<li>o：小写，在光标当前行的<strong>下一行插入新行</strong></li>
<li>O：大写，在光标当前行的<strong>上一行插入新行</strong></li>
</ul>
<h3 id="替换和取消"><a href="#替换和取消" class="headerlink" title="替换和取消"></a>替换和取消</h3><ul>
<li>yy：复制当前行；nyy：复制n行</li>
<li>p：粘贴</li>
<li>u：undo，取消上一步操作</li>
<li>Ctrl + r：redo，返回到undo之前，即撤销撤销</li>
<li>r：替换光标所在处的字符</li>
<li>R：从光标处开始替换，按Esc后结束</li>
</ul>
<h3 id="删除命令"><a href="#删除命令" class="headerlink" title="删除命令"></a>删除命令</h3><ul>
<li>x：删除光标所在字符</li>
<li>nx：删除光标所在处后的n个字符</li>
<li>dd：删除光标所在行</li>
<li>D：删除光标所在处到行尾的所有内容</li>
<li>ndd：删除光标所在行开始的n行</li>
<li>dG：删除光标所在行到末尾行的所有内容</li>
<li>:5,7d：删除指定范围的行，此处是删除第5、7行</li>
</ul>
<h3 id="相关快捷键"><a href="#相关快捷键" class="headerlink" title="相关快捷键"></a>相关快捷键</h3><ul>
<li>Shift + zz：保存退出，与<code>:wq</code>命令作用相同</li>
<li>v：进入字符可视模式，选择单个字符，按y复制，按p粘贴</li>
<li>V或Shift + v：进入行可视模式，选择行，按y复制，按p粘贴</li>
<li>Ctrl + v：进入块可视模式，选择块，按y复制，按p粘贴</li>
</ul>
<h2 id="Linux用户和权限"><a href="#Linux用户和权限" class="headerlink" title="Linux用户和权限"></a>Linux用户和权限</h2><blockquote>
<p>Linux操作系统是一个多用户操作系统，允许多用户同时登录到系统上并使用资源。系统会根据账户来区分每个用户的文件，任务和工作环境，使得每个用户工作都不受干扰。</p>
</blockquote>
<h3 id="用户类别"><a href="#用户类别" class="headerlink" title="用户类别"></a>用户类别</h3><ul>
<li>超级用户：root，UID=0</li>
<li>伪用户：UID在1-499<ul>
<li>伪用户不能登录系统，且没有宿主目录</li>
<li>如系统和服务相关的：bin、daemon、shutdown等</li>
<li>进程相关的：mail、news、games等</li>
</ul>
</li>
<li>普通用户：UID在500-60000</li>
</ul>
<h3 id="配置文件"><a href="#配置文件" class="headerlink" title="配置文件"></a>配置文件</h3><ul>
<li><p><strong><code>/etc/passwd</code>：保存用户信息的文件</strong></p>
<p>使用<code>cat /etc/passwd</code>查看文件，内容<strong>格式</strong>为：</p>
<img src="/imgs/linux/image-20201219171544970.png" alt="用户文件内容" style="zoom:67%;" />

<ul>
<li>root - 用户名：用户登录系统的用户名</li>
<li>x - 密码：密码位</li>
<li>0 - UID：用户标识号</li>
<li>0 - GID：默认组标识号</li>
<li>root - 描述信息：存放用户的描述信息</li>
<li>/root - 宿主目录：用户登录系统的默认目录，默认是在/home/下</li>
<li>/bin/bash - 命令解析器：用户使用的Shell，默认是bash</li>
</ul>
</li>
<li><p><strong><code>/etc/shadow</code>：保存密码的文件</strong></p>
<p>使用命令<code>cat /etc/shadow</code>查看，内容格式如图所示，依次表示含义为：</p>
<img src="/imgs/linux/image-20201219172418997.png" alt="用户密码文件内容" style="zoom:67%;" />

<ul>
<li>用户名：登录系统的用户名</li>
<li>密码：加密密码</li>
<li>最后一次修改时间：最后一次修改密码距离现今的天数</li>
<li>最小时间间隔：两次修改密码间隔最小时间</li>
<li>最大时间间隔：~</li>
<li>警告时间</li>
<li>账号限制时间</li>
<li>失效时间</li>
<li>标志</li>
</ul>
</li>
<li><p><strong><code>/etc/group</code>：保存用户组的文件</strong></p>
<blockquote>
<p>每个用户至少属于一个组；每个组可以包含多个用户；同一个组的用户拥有组的权限；</p>
</blockquote>
<img src="/imgs/linux/image-20201219173014652.png" alt="用户组文件内容" style="zoom:67%;" />

<ul>
<li>组名：</li>
<li>组密码：密码位，一般不使用</li>
<li>GID：组标识号</li>
<li>组内用户列表：属于改组的用户的列表</li>
</ul>
</li>
<li><p><code>/etc/gshadow</code>：保存用户组密码的文件</p>
</li>
<li><p><code>/etc/default/useradd</code>：用户配置文件</p>
</li>
</ul>
<h3 id="操作用户命令"><a href="#操作用户命令" class="headerlink" title="操作用户命令"></a>操作用户命令</h3><ul>
<li><code>useradd</code> ：<strong>添加用户</strong><ul>
<li>-u：uid，指定用户标识ID</li>
<li>-g：gid，指定所属的组名ID</li>
<li>-G：Groups，指定多个组，使用逗号， 间隔</li>
<li>-c：comment，用户描述</li>
<li>-e：expire date，失效时间</li>
<li>例如创建用户du的命令：<code>useradd -u 888 -g users -G sys,root -c &quot;hr du&quot; du passwd du</code> </li>
</ul>
</li>
<li><code>usermod</code>：user modify，<strong>修改用户</strong><ul>
<li>-l：修改用户名，如将用户a名称改为b的命令是：<code>usermod -l a b</code> </li>
<li>-g：添加组，为用户tom添加sys组：<code>usermod -g sys tom</code> </li>
<li>-G：添加多个组，使用逗号间隔</li>
<li>-L：Lock，锁定账户密码</li>
<li>-U：Unlock，解锁账户密码</li>
</ul>
</li>
<li><code>userdel</code>：user delete，<strong>删除用户</strong><ul>
<li>-r：remove，删除账号同时删除账号相关目录 </li>
</ul>
</li>
</ul>
<h3 id="操作组命令"><a href="#操作组命令" class="headerlink" title="操作组命令"></a>操作组命令</h3><ul>
<li>groups：显示用户所属组</li>
<li>groupadd：添加组<ul>
<li>-g：指定组gid</li>
</ul>
</li>
<li>groupmod：修改组<ul>
<li>-n：new group，修改组名</li>
</ul>
</li>
<li>groupdel：删除组</li>
</ul>
<h3 id="权限操作命令"><a href="#权限操作命令" class="headerlink" title="权限操作命令"></a>权限操作命令</h3><p><strong>三种基本权限：</strong>r（read）读权限；w（write）写权限；x（execute）执行权限；</p>
<img src="/imgs/linux/image-20201219195144607.png" alt="文件权限信息" style="zoom:67%;" />

<ul>
<li>第1位：表示文件类型，d-目录，-普通文件，I-链接文件</li>
<li>第2-4位：所属用户权限，用u（user）表示</li>
<li>第5-7位：所属组权限，用g（group）表示</li>
<li>第8-10位：其他用户权限，用o（other）表示</li>
<li>2-10位标识所有的权限，用a（all）表示</li>
<li><code>-rwxrw-r--</code>：表示一个普通文件，所属用户有读/写/执行权限，同组用户有读/写权限，其他组只有读权限</li>
</ul>
<p><strong>更改文件权限操作：</strong></p>
<ul>
<li>chmod：change mode，修改文件权限<ul>
<li>-R：Receusive，表示其下的文件和子目录做相同的权限操作</li>
<li>如：<code>chmod u+x a.txt</code>  ：为a.txt文件的所属用户添加执行权限</li>
</ul>
</li>
<li>使用数字来表示权限：r=4，w=2，x=1，-=0<ul>
<li>如：<code>chmod 750 b.txt</code>：表示文件b.txt，所属用户有读/写/执行权限，所属组有读/执行/权限，其他无</li>
</ul>
</li>
</ul>
<h2 id="Linux中安装软件"><a href="#Linux中安装软件" class="headerlink" title="Linux中安装软件"></a>Linux中安装软件</h2><h3 id="RPM"><a href="#RPM" class="headerlink" title="RPM"></a>RPM</h3><blockquote>
<p>RedHat Package Manager，RedHat软件包管理工具</p>
</blockquote>
<p>相关参数：</p>
<ul>
<li>i：install，安装应用程序</li>
<li>e：erase，卸载应用程序</li>
<li>vh：verbose hash，显示安装进度</li>
<li>U：update，升级软件包</li>
<li>qa：query all，显示所有已安装软件包</li>
<li>结合grep命令使用</li>
</ul>
<h3 id="YUM"><a href="#YUM" class="headerlink" title="YUM"></a>YUM</h3><blockquote>
<p>Yellow dog Updater，Modified  是一个在Fedora和RedHat以及SUSE、CentOS中的Shell前端软件包管理器。</p>
</blockquote>
<ul>
<li><p>yum instll gcc-c++</p>
</li>
<li><p>yum remove gcc-c++</p>
</li>
<li><p>yum update gcc-c++</p>
</li>
<li><p>yum 安装软件时出现 is this ok [y/d/n]：</p>
<ul>
<li>y：下载安装</li>
<li>n：不安装</li>
<li>d：只下载不安装</li>
</ul>
</li>
</ul>
<h3 id="安装JDK"><a href="#安装JDK" class="headerlink" title="安装JDK"></a>安装JDK</h3><ol>
<li><p><a href="https://www.jdkdownload.com/">JDK下载</a> </p>
</li>
<li><p>将<code>jdk-8u181-linux-x64.tar.gz</code>传至Linux系统中</p>
<ul>
<li>使用FTP连接Linux进行文件传输，前提是Linux系统要安装FTP服务，且开启FTP的21端口</li>
<li>在XShell中使用命令传输文件，使用 <code>rz</code> 命令，实现windows项Linux传输文件，选择单个文件</li>
<li>使用<code>sz</code>命令，实现从Linux向windows传输文件</li>
</ul>
</li>
<li><p>解压缩，命令 <code>tar -zxvf jdk-8u181-linux-x64.tar.gz /usr/local </code> 解压缩到<code>/usr/local</code>中 </p>
</li>
<li><p>配置环境变量</p>
<ol>
<li><p>使用命令<code>vi /etc/profile</code> 编辑系统环境变量设置</p>
</li>
<li><p>最后追加内容：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export JAVA_HOME&#x3D;&#x2F;usr&#x2F;local&#x2F;jdk1.7.0_45</span><br><span class="line">export PATH&#x3D;$PATH:$JAVA_HOME&#x2F;bin</span><br></pre></td></tr></table></figure>
</li>
<li><p>刷新环境变量 <code>source /etc/profile</code> </p>
</li>
</ol>
</li>
<li><p>测试JDK环境，命令：<code>java -version</code> ，显示当前版本号即表示成功</p>
<img src="/imgs/linux/image-20201219204628690.png" alt="image-20201219204628690" style="zoom:67%;" />

</li>
</ol>
<h3 id="安装Tomcat"><a href="#安装Tomcat" class="headerlink" title="安装Tomcat"></a>安装Tomcat</h3><ol>
<li><p>上传<code>apache-tomcat-7.0.68.tar.gz</code>到Linux系统</p>
</li>
<li><p>解压缩文件到指定目录，<code>tar -zxvf apache-tomcat-7.0.68.tar.gz -C /usr/local</code> </p>
</li>
<li><p>启动tomcat，进入tomcat/bin目录下，执行启动文件<code>./startup.sh</code> </p>
<img src="/imgs/linux/image-20201220100649965.png" alt="启动tomcat" style="zoom:67%;" />
</li>
<li><p>显示启动后使用命令查看tomcat是否已经启动成功：</p>
<ol>
<li><p><code>netstat -nltp</code>：查看系统当前监听的端口情况</p>
</li>
<li><p>8080端口开启，表示启动成功</p>
<img src="/imgs/linux/image-20201220094551030.png" alt="tomcat运行成功" style="zoom:67%;" />
</li>
<li><p>此时访问ip:8080可以看到如下，则表示tomcat安装成功。</p>
<img src="/imgs/linux/image-20201220101613257.png" alt="image-20201220101613257" style="zoom:50%;" />
</li>
</ol>
</li>
<li><p><strong>Tomcat启动时常见问题</strong></p>
<ol>
<li><p>如果8080端口没有监听，说明tomcat启动失败</p>
<img src="/imgs/linux/image-20201220094454339.png" alt="tomcat启动失败" style="zoom:67%;" />
</li>
<li><p>若tomcat启动失败，查看tomcat日志</p>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> tomcat/logs</span><br><span class="line">cat catalina.out	#查看日志文件</span><br></pre></td></tr></table></figure>
</li>
<li><p>日志文件内容如下：表示启动时tomcat没有找到指定的Java环境</p>
<img src="/imgs/linux/image-20201220101025996.png" alt="tomcat启动失败日志信息" style="zoom:67%;" />
</li>
<li><p>检查配置环境变量时JDK路径错误，修改后重新启动tomcat，查看端口情况如下：启动成功</p>
<img src="/imgs/linux/image-20201220094551030.png" alt="tomcat运行成功" style="zoom:67%;" />
</li>
<li><p>如果tomcat已经启动，外部访问仍然失败，则要检查防火墙是否关闭或者是否允许8080端口通行。</p>
</li>
</ol>
</li>
</ol>
<h3 id="安装MySQL"><a href="#安装MySQL" class="headerlink" title="安装MySQL"></a>安装MySQL</h3><blockquote>
<p>yum安装是最简单的，使用压缩包安装失败，因此换用了yum方式。 <a href="https://blog.csdn.net/ntuxiaolei/article/details/81777335?utm_medium=distribute.pc_relevant_t0.none-task-blog-BlogCommendFromMachineLearnPai2-1.control&depth_1-utm_source=distribute.pc_relevant_t0.none-task-blog-BlogCommendFromMachineLearnPai2-1.control">参考教程</a> </p>
</blockquote>
<ol>
<li><p>查看是否使用yum安装过mysql，使用命令：<code>rpm -qa | grep -i mysql</code> </p>
<ul>
<li><p>如下图显示已经安装的相关模块，则要先卸载，再重新安装</p>
<img src="/imgs/linux/image-20201221195202378.png" alt="image-20201221195202378" style="zoom:80%;" />

<ul>
<li><code>yum -y remove &lt;全名称&gt;</code>：卸载相关模块</li>
<li>如果上述命令卸载不掉，则使用命令： <code>rpm -er &lt;全名称&gt;</code> </li>
</ul>
</li>
<li><p>如果执行后无任何变化，则表示没有安装，可以正常进行</p>
</li>
</ul>
</li>
<li><p>使用yum命令下载并安装mysql相关服务</p>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line"># <span class="built_in">cd</span> /usr/local		#进入/usr/local文件夹下</span><br><span class="line"># wget http://repo.mysql.com/mysql57-community-release-el7-<span class="number">8</span>.noarch.rpm #下载.rpm文件</span><br><span class="line"># rpm -ivh mysql57-community-release-el7-<span class="number">8</span>.noarch.rpm #</span><br><span class="line"># yum -y install mysql-server	#安装mysql服务</span><br><span class="line">#也可以指定安装目录</span><br><span class="line"># yum --install root=/usr/local/mysql --releaserver=/ -y install mysql-server</span><br></pre></td></tr></table></figure>
</li>
<li><p>等待所有服务模块安装完成</p>
<img src="/imgs/linux/image-20201221203743558.png" alt="image-20201221203743558" style="zoom:67%;" />
</li>
<li><p>mysql相关配置文件路径，并配置mysql的配置文件</p>
<ul>
<li><p>配置文件：<code>/etc/my.cnf</code></p>
</li>
<li><p>日志文件：<code>/var/log/mysqld.log</code></p>
</li>
<li><p>服务启动脚本：<code>/usr/lib/systemd/system/mysqld.service</code></p>
</li>
<li><p>socket文件：<code>/var/run/mysqld/mysqld.pid</code></p>
</li>
<li><p>添加mysl配置内容：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[mysqld]</span><br><span class="line">datadir&#x3D;&#x2F;var&#x2F;lib&#x2F;mysql</span><br><span class="line">socket&#x3D;&#x2F;var&#x2F;lib&#x2F;mysql&#x2F;mysql.sock</span><br><span class="line">server_id &#x3D; 1</span><br><span class="line">expire_logs_days &#x3D; 3</span><br><span class="line"> </span><br><span class="line"># Disabling symbolic-links is recommended to prevent assorted security risks</span><br><span class="line">symbolic-links&#x3D;0</span><br><span class="line"> </span><br><span class="line">log-error&#x3D;&#x2F;var&#x2F;log&#x2F;mysqld.log</span><br><span class="line">pid-file&#x3D;&#x2F;var&#x2F;run&#x2F;mysqld&#x2F;mysqld.pid</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>启动mysql服务，相关命令：</p>
<ul>
<li><code>systemctl start mysqld.service</code>：开启mysql服务</li>
<li><code>system status mysqld.service</code>：查看mysql服务状态，<ul>
<li>-l：查看详细状态信息</li>
</ul>
</li>
<li><code>systemctl stop mysql.service</code>：停止mysql服务</li>
<li><code>systemctl restart mysql.service</code>：重启mysql服务</li>
</ul>
</li>
<li><p>设置mysql开机自启动</p>
<ul>
<li><code>systemctl enable mysqld</code>：</li>
<li><code>systemctl daemon-reload</code>：</li>
</ul>
</li>
<li><p><strong>修改mysql密码</strong></p>
<blockquote>
<p>安装mysql后，会在相应的日志文件中产生一个随机密码，可以使用命令查看</p>
<ul>
<li>cat /var/log/mysqld.log    ：查看日志文件全部内容</li>
<li>grep “password” /var/log/mysqld.log   ：正则匹配日志文件中的password关键字</li>
</ul>
</blockquote>
<p>使用命令登录mysql：<code>mysql -u root -p</code>：命令回车后输入日志中密码，登录mysql</p>
<ul>
<li><p>登录成功，修改密码：<code>ALTER USER &#39;root&#39;@&#39;localhost&#39; IDENTIFIED BY &#39;rOOT@2020&#39;;</code>即root账户的密码修改为Root@2020</p>
</li>
<li><p>随机密码登录失败时，需要配置跳过认证阶段，登录后使用sql语句更新密码</p>
<ol>
<li><p>修改配置文件：<code>vi /etc/my.cnf</code> ，添加 skip-grant-tables 保存并退出，表示登录不验证</p>
</li>
<li><p>重启mysql服务，使用mysql直接登录mysql客户端</p>
</li>
<li><p>使用sql语句修改登录密码</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#update user set password&#x3D;password(&quot;Root@2020&quot;) where user&#x3D;&quot;root&quot;;</span><br><span class="line">#mysql 5.7的数据库没有了password字段 用的是authentication_string字段</span><br><span class="line">use mysql;</span><br><span class="line">update mysql.user set authentication_string&#x3D;password(&#39;Root@2020&#39;) where user&#x3D;&#39;root&#39; ;</span><br><span class="line">flush privileges;	#</span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
<li><p><strong>修改密码策略</strong>（默认密码策略要求密码是大小写字母+数字+特殊字符的组合且最少8位）</p>
</li>
</ul>
</li>
<li><p>设置允许远程登录</p>
</li>
<li><p>设置允许其他端，navicate远程连接</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mysql -u root -p Root@2020	#连接客户端</span><br><span class="line">mysql&gt; use mysql;</span><br><span class="line">mysql&gt; ALTER USER &#39;root&#39;@&#39;%&#39; IDENTIFIED WITH mysql_native_password BY &#39;Root@2020&#39;;</span><br><span class="line">#如果上句运行失败，则使用下面命令</span><br><span class="line">#mysql&gt; grant all on *.* to root@&quot;%&quot; identified by &quot;Root@2020&quot;;</span><br><span class="line">mysql&gt; flush privileges;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="安装使用Redis"><a href="#安装使用Redis" class="headerlink" title="安装使用Redis"></a>安装使用Redis</h3><p>安装步骤： <a href="https://wsk1103.github.io/2019/01/12/centos-7-%E5%AE%89%E8%A3%85Redis/">安装教程</a> </p>
<ol>
<li><p>如果电脑中没有安装过gcc依赖，则要先安装才行（否则编译时报错）</p>
<blockquote>
<p><code>$ yum -y install gcc</code></p>
</blockquote>
</li>
<li><p>可以直接使用命令安装，但是安装的版本不能指定，一般就是比较低的版本</p>
<blockquote>
<p><code># yum install redis</code></p>
</blockquote>
</li>
<li><p>使用命令下载文件后进行安装  进入到 usr/local 文件夹中下载</p>
<blockquote>
<p><code># wget http://download.redis.io/releases/redis-5.0.4.tar.gz</code></p>
</blockquote>
</li>
<li><p>解压压缩包  <code># tar zxvf redis-5.0.4.tar.gz</code> </p>
</li>
<li><p>解压后进入到 redis-5.0.4 目录下，进行编译</p>
<ol>
<li>先编译库  <code># make MALLOC=libc</code></li>
<li>再编译资源（安装） <code># cd src &amp;&amp; make install</code></li>
</ol>
</li>
<li><p>编译安装完成，可以启动redis服务了</p>
</li>
<li><p>为了使用redis更加方便，需要对redis进行一定的配置，redis-5.0.4下的redis.conf文件</p>
</li>
<li><p>将配置文件复制到etc文件夹下  <code>cp redis.conf /etc</code> ，然后进行更改（也可更改后复制）</p>
<blockquote>
<p><code># vi redis.conf</code>     etc是系统配置文件存放的目录，方便配置</p>
</blockquote>
<ol>
<li>允许其他ip访问 ：将 <code>bind 127.0.0.1</code> 注释掉，此处限制了访问redis的地址</li>
<li>解除安全保护机制：修改 <code>protected-mode yes</code> 为 <code>protected-mode no</code></li>
<li>修改redis启动为守护进程：修改 <code>daemonize no</code> 为  <code>daemonize yes</code> </li>
</ol>
</li>
<li><p><strong>启动修改后redis服务   <code>redis-server /etc/redis.conf</code></strong> 👍</p>
<img src="/../../../markdownLog/%25E9%259D%25A2%25E8%25AF%2595%25E6%2589%25BE%25E5%25B7%25A5%25E4%25BD%259C/assets/image-20201101215817433.png" alt="image-20201101215817433" style="zoom:80%;" />
</li>
<li><p>服务启动后，可以使用命令启动客户端进行测试  <code>redis-cli</code> </p>
<ol>
<li><p>模拟客户端发起通信 <code>redis-cli</code></p>
</li>
<li><p>如果不成功，要退出后重新进行 <code>exit</code></p>
<img src="/../../../markdownLog/%25E9%259D%25A2%25E8%25AF%2595%25E6%2589%25BE%25E5%25B7%25A5%25E4%25BD%259C/assets/image-20201101215939224.png" alt="image-20201101215939224" style="zoom:80%;" />
</li>
</ol>
</li>
<li><p>设置redis启动服务脚本（使用systemd管理redis）</p>
<ol>
<li><p>在 <code>/usr/lib/systemd/system</code> 目录下新建文件 <code>redis.service</code> ，并添加内容如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[Unit]</span><br><span class="line">Decription&#x3D;Redis 6379</span><br><span class="line">After&#x3D;syslog.target network.target</span><br><span class="line">[Service]</span><br><span class="line">Type&#x3D;forking</span><br><span class="line">PrivateTmp&#x3D;yes</span><br><span class="line">Restart&#x3D;always</span><br><span class="line">ExecStart&#x3D;&#x2F;usr&#x2F;local&#x2F;bin&#x2F;redis-server &#x2F;etc&#x2F;redis.conf</span><br><span class="line">ExecStop&#x3D;&#x2F;usr&#x2F;local&#x2F;bin&#x2F;redis-cli -h 127.0.0.1 -p 6379 -a jcon shutdown</span><br><span class="line">User&#x3D;root</span><br><span class="line">Group&#x3D;root</span><br><span class="line">LimitCORE&#x3D;infinity</span><br><span class="line">LimitNOFILE&#x3D;100000</span><br><span class="line">LimitNPROC&#x3D;100000</span><br><span class="line">[Install]</span><br><span class="line">WantedBy&#x3D;multi-user.target</span><br></pre></td></tr></table></figure>
</li>
<li><p>设置完毕后，可以使用系统命令开启关闭redis</p>
</li>
</ol>
</li>
</ol>
<pre><code>3. 好像没有成功，等等再试</code></pre>
<ol start="12">
<li><p>设置redis开机自启动</p>
<ol>
<li><p>复制一份 <strong>utils/redis_init_script</strong> 到 <strong>/etc/init.d/</strong></p>
</li>
<li><p>修改 <code>redis_init_script</code> 文件，指定具体的启动文件</p>
<img src="/../../../markdownLog/%25E9%259D%25A2%25E8%25AF%2595%25E6%2589%25BE%25E5%25B7%25A5%25E4%25BD%259C/assets/image-20201102094652970.png" alt="image-20201102094652970" style="zoom:80%;" />
</li>
<li><p>设置自启动命令 <code># chkconfig redis_init_script on</code> ，</p>
</li>
<li><p>如果关闭自启动，将on换成off</p>
</li>
</ol>
</li>
<li><p>修改Redis的密码</p>
<img src="/../../../markdownLog/%25E9%259D%25A2%25E8%25AF%2595%25E6%2589%25BE%25E5%25B7%25A5%25E4%25BD%259C/assets/image-20201102094737334.png" alt="image-20201102094737334" style="zoom:80%;" />
</li>
<li><p>redis<a href="https://juejin.im/post/6844903889242685454">安装过程常见错误</a> </p>
</li>
</ol>
<h3 id="安装Niginx"><a href="#安装Niginx" class="headerlink" title="安装Niginx"></a>安装Niginx</h3><p><a href="https://blog.csdn.net/weixin_38118016/article/details/89949131">教程–安装测试成功！</a> </p>
<ol>
<li>将nginx加入到yum中</li>
<li>在yum中安装nginx</li>
<li>安装完成测试本地访问</li>
<li>使用外网访问（关闭防火墙）</li>
</ol>
<p>Nginx 的相关操作命令：</p>
<p># systemctl start nginx.service      // 启动 Nginx 服务<br># systemctl stop nginx.service      // 停止 Nginx 服务<br># systemctl restart nginx.service    // 重启 Nginx 服务<br># systemctl reload nginx.service    // 重新加载 Nginx 配置文件，然后以优雅的方式重启 Nginx<br># systemctl status nginx.service    // 查看状态<br># systemctl enable nginx.service     // 设置开机自启动<br># systemctl disable nginx.service   // 停止开机自启动</p>
<ul>
<li>nginx -V：（查看版本信息）</li>
<li>查看nginx的状态及进程、端口<ul>
<li>1、systemctl status nginx.service    （nginx服务状态）</li>
<li>2、netstat -antp | grep :                    （查看所有端口的占用情况）</li>
<li>3、netstat -antp | grep :80                （查看80端口占用）</li>
<li>4、ps aux | grep nginx                       （查看nginx进程运行状态）</li>
</ul>
</li>
<li>杀掉进程<ul>
<li>1、killall -9 nginx                                 （杀死所有nginx相关进程）</li>
<li>2、kill -9 pid1 and kill -9 pid1            （杀掉两个制定pid的进程）</li>
</ul>
</li>
</ul>
<p><strong>nginx配置文件路径：/etc/nginx/conf.d/default.conf</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;源文件内容</span><br><span class="line">server &#123;</span><br><span class="line">    listen       80;</span><br><span class="line">    server_name  localhost;</span><br><span class="line"></span><br><span class="line">    #charset koi8-r;</span><br><span class="line">    #access_log  &#x2F;var&#x2F;log&#x2F;nginx&#x2F;host.access.log  main;</span><br><span class="line"></span><br><span class="line">    location &#x2F; &#123;</span><br><span class="line">        root   &#x2F;usr&#x2F;share&#x2F;nginx&#x2F;html;</span><br><span class="line">        index  index.html index.htm;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    #error_page  404              &#x2F;404.html;</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F;修改后内容</span><br><span class="line">server &#123;</span><br><span class="line">    listen       80;	&#x2F;&#x2F;要监听的端口，即访问时 url:port</span><br><span class="line">    server_name  localhost;</span><br><span class="line"></span><br><span class="line">    #charset koi8-r;</span><br><span class="line">    #access_log  &#x2F;var&#x2F;log&#x2F;nginx&#x2F;host.access.log  main;</span><br><span class="line">    </span><br><span class="line">    #文件上传大小限制</span><br><span class="line">    client_max_body_size    1000m;</span><br><span class="line">    </span><br><span class="line">    #location &#x2F; &#123;</span><br><span class="line">    #    root   &#x2F;usr&#x2F;share&#x2F;nginx&#x2F;html;</span><br><span class="line">    #    index  index.html index.htm;</span><br><span class="line">    #&#125;</span><br><span class="line">    #此处修改针对不同请求url分发到不同服务地址</span><br><span class="line">	location ~ &#x2F;eduservice&#x2F;&#123;</span><br><span class="line">		proxy_pass http:&#x2F;&#x2F;localhost;</span><br><span class="line">	&#125;</span><br><span class="line">	#对象存储</span><br><span class="line">	location ~ &#x2F;eduoss&#x2F;&#123;</span><br><span class="line">		proxy_pass http:&#x2F;&#x2F;localhost;</span><br><span class="line">	&#125;</span><br><span class="line">	#视频点播资源</span><br><span class="line">	location ~ &#x2F;eduvod&#x2F;&#123;</span><br><span class="line">		proxy_pass http:&#x2F;&#x2F;localhost;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    #error_page  404              &#x2F;404.html;</span><br></pre></td></tr></table></figure>

<h3 id="安装Docker"><a href="#安装Docker" class="headerlink" title="安装Docker"></a>安装Docker</h3><h1 id="Linux（Ubuntu）（实验室服务器）"><a href="#Linux（Ubuntu）（实验室服务器）" class="headerlink" title="Linux（Ubuntu）（实验室服务器）"></a>Linux（Ubuntu）（实验室服务器）</h1><p><a href="https://jingyan.baidu.com/article/acf728fd440982f8e510a3a9.html">ubuntu修改登录密码</a> </p>
<p>du:123456</p>
<p>root:root</p>
<p>win本地使用Xshell连接远程Linux系统时，连接失败。如果本地对linux可以ping通，远程ping本地不通，则应该是：（解决后连接成功）</p>
<p><img src="/imgs/linux/image-20201120122929012.png" alt="image-20201120122929012"></p>
]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
</search>
